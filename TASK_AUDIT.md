# TASK_AUDIT.md — аудит состояния репозитория

Папка проекта: C:\Users\awf\Desktop\test

Это задание на ПРОВЕРКУ, а не на разработку. Ничего не улучшай по своей
инициативе. Порядок: сначала весь аудит, потом отчёт, потом ждёшь моего слова.

## Контекст

Прочитай в таком порядке, прежде чем что-либо делать:
1. `AGENTS.md` — правила проекта, стек, методология. Они обязательны.
2. `ASSUMPTIONS.md` — что уже проверено, что предположение.
3. `TASK.md` — задание, которое предыдущий агент должен был выполнить.
4. `README.md`, `requirements.txt`, `probe.py`.

## Что уже известно (не перепроверяй, это установленный факт)

Предыдущий прогон **не выполнил ничего**. Проверено по архиву:
- `pm/experiments/e2_fee_basis.py` — отсутствует.
- `PROGRESS.md`, `DECISIONS_NEEDED.md`, `PREREGISTRATION.md`, `PROBE_RESULTS.md`
  — отсутствуют.
- `data/` пустая.
- `git log` содержит ровно 3 коммита, все от первичной настройки:
  `fa9577d`, `cdb2c2e`, `317506f`. Ни одного коммита от агента.
- Незакоммиченными лежат только исходные файлы распакованного архива.
- `hybrid_by_spread` в коде не встречается.
- Кластеризации по турнир-дню в коде нет.
- `pydantic-settings==2.7.0` в `requirements.txt` есть — но это было в архиве
  изначально, а не добавлено агентом.

Твоя задача — не подтвердить это заново, а **проверить, что репозиторий
работоспособен**, и составить точный список того, что предстоит сделать.

## Шаг 1. Работоспособность

```
python -m unittest discover -s tests -v
python -c "import probe"
```

Ожидание: 35 тестов проходят; `import probe` **падает** с
`ModuleNotFoundError: No module named 'pm.experiments.e2_fee_basis'`
либо на `pydantic_settings`, если окружение не установлено.

Если `import probe` падает — это блокер №1. Запиши точный текст ошибки
и номер строки. Не чини на этом шаге.

## Шаг 2. Инвентаризация обязанностей probe.py

Выпиши, что именно `probe.py` импортирует из `pm.experiments.e2_fee_basis`:
имена функций, их вызовы, ожидаемые типы аргументов и возврата. Ищи по
строкам 41, 186, 187, 193 и в `pm/experiments/__init__.py` строка 9.

Это нужно, чтобы отсутствующий модуль потом был написан под уже
существующие вызовы, а не наоборот.

## Шаг 3. Нумерация гейтов

Сейчас нумерация противоречива. Правильная схема:

```
G1 — семантика цены (Э1): mid или last trade
G2 — базис комиссии (Э2): shares или notional
G3 — период покрыт СВОИМИ снимками стакана
G4 — мощность выборки достаточна (Э4)
```

Найди все места, где нумерация нарушена:
```
grep -rn "G1\|G2\|G3\|G4" . --include=*.py --include=*.md
```

Известная ошибка: в `pm/experiments/e4_tennis.py` строка 102 функция
`power_note` возвращает текст «Гейт G1 НЕ ПРОЙДЕН» для гейта мощности.
Это G4, а не G1. Найди остальные такие места сам.

Ничего не правь. Просто список: файл, строка, что написано, что должно быть.

## Шаг 4. Гейт мощности — проверка на «не может сработать»

Посмотри, с чем сравнивается порог `MIN_EVENTS_PER_TRADER = 30` в
`e4_tennis.py`. Ответь письменно на один вопрос:

**Сравнивается ли общее число разрешённых событий на рынке с порогом,
который по смыслу относится к одному трейдеру?**

Если да — этот гейт не может сработать никогда, потому что событий на рынке
всегда сильно больше 30. Тогда он не гейт, а украшение. Запиши это.

## Шаг 5. Мелкие баги — только зафиксировать

Проверь и опиши, не исправляя:
1. `resolved_in_window` в `e4_tennis.py` (строки 147-159): цикл возвращает
   результат на первом же ключе, у которого значение — строка. Если этот
   первый ключ не тот, остальные три ключа не проверяются никогда.
   Подтверди или опровергни.
2. `iter_markets` в `pm/markets.py`: `page_limit=500`, `max_pages=40`.
   Посчитай, сколько это запросов и укладывается ли в лимит Gamma
   300 запросов / 10 секунд.
3. `pm/store.py`: DDL для таблиц `e1_points`, `e1_pair_sums`,
   `e4_tennis_markets`. Найди, вызывается ли `connect()` хоть откуда-то.
   Если нет — это мёртвый код.

## Шаг 6. Отчёт

Создай `AUDIT_REPORT.md` со структурой:

```
# AUDIT_REPORT
Дата:
Коммит на момент аудита: <git rev-parse HEAD>

## 1. Тесты
Результат unittest:
Результат import probe:

## 2. Контракт отсутствующего модуля e2_fee_basis
<имена, сигнатуры, места вызова>

## 3. Нарушения нумерации гейтов
| файл | строка | сейчас | должно быть |

## 4. Гейт мощности
Может ли сработать: да / нет
Обоснование:

## 5. Мелкие баги
1. resolved_in_window:
2. пагинация Gamma:
3. мёртвый DDL в store.py:

## 6. Что нужно сделать (упорядочено по блокирующей силе)
1.
2.
...

## 7. Вопросы, на которые я не имею права ответить сам
<если таких нет — так и напиши>
```

## Жёсткие запреты на это задание

- Не создавай и не изменяй ни одного `.py` файла.
- Не запускай `git commit` и `git push`.
- Не запускай `probe.py` — там сетевые вызовы и лимиты, это отдельный шаг.
- Не трогай `.env`.
- Не ставь пакеты вне `requirements.txt`.
- Не создавай `pm/broker.py`. Он отсутствует намеренно.
- Не предлагай изменения критериев, порогов и исходов методологии.

## Готово когда

`AUDIT_REPORT.md` существует, все семь разделов заполнены, ни один файл
кода не изменён (`git status --short` показывает только новый
`AUDIT_REPORT.md`).
