# ORIENTATION.md — что это за проект

Читай этот файл первым в любой новой сессии. Он отвечает на вопрос
«где я и что тут происходит». Правила — в `AGENTS.md`, они важнее.

## В одном абзаце

Один человек строит личный аналитический слой над Polymarket. Три цели,
в порядке надёжности: (A) свой терминал вместо платной подписки,
(B) проверить, есть ли у теннисных трейдеров КОПИРУЕМЫЙ край,
(C) опубликовать датасет по чистоте рынка.

Цель B — гипотеза, а не план. Ответ «края нет» — успешный исход проекта,
а не провал. Никогда не подгоняй анализ под положительный ответ.

## Где мы сейчас

Сделан Этап 0 (разведка): собран репозиторий `probe.py` + `pm/`,
который должен закрыть четыре гейта. Ни один гейт пока не закрыт,
`probe.py` не запускался ни разу, данных нет.

Критический путь дальше:
```
гейты G1+G2 -> свой сборщик стакана -> CLV-скрин -> копируемость -> вердикт
```

## Четыре гейта

Гейт — это вопрос о реальности, без ответа на который всё последующее
число — мусор. Гейты соединены И. Любой незакрытый запрещает исход GO.

| Гейт | Вопрос | Где закрывается |
|---|---|---|
| G1 | Отображаемая цена — это mid или цена последней сделки? | Э1 |
| G2 | В `fee = C * rate * p * (1-p)` величина C — шары или нотионал? | Э2 |
| G3 | Есть ли у нас СВОИ снимки стакана за период анализа? | Этап 1 |
| G4 | Хватает ли данных, чтобы вообще различить гипотезы? | Э4 |

Почему G1 важен: CLV — это разница между ценой входа и ценой закрытия.
Если окажется, что «цена» в API — это цена последней сделки, а не середина
стакана, то весь измеренный край может оказаться артефактом спреда.

Почему G2 важен: при цене 0.10 две гипотезы дают издержки, различающиеся
в 10 раз. Найденный край обычно меньше этой разницы.

## Главное решающее правило

```
GO  только если  gross_delta - cost > 2.5 * sqrt(SE_edge^2 + SE_cost^2)
```

Под корнем СУММА, не max. Коэффициент 2.5 не меняется никогда.

Порядок проверки исходов фиксирован:
1. `bracket_width > gross_delta` -> **UNDECIDABLE**. Проверяется ПЕРВЫМ.
2. Иначе неравенство выполнено -> **GO**.
3. Иначе -> **NO-GO**.

Нельзя превратить UNDECIDABLE в GO, выбрав удобный край вилки издержек.
Единственное лечение UNDECIDABLE — закрыть гейт и пересчитать.

## Семь фильтров отбора трейдеров (порядок фиксирован)

0. Берём всю популяцию, НЕ лидерборд. Лидерборд уже отобран по удаче.
1. >= 100 матчей и >= 60 дней истории.
2. Прошёл детекторы чистоты (wash-trading, фарм стимулов).
3. Нет зомби-позиций, искажающих PnL.
4. Концентрация: `max_market_pnl / total_pnl < 0.25`.
5. CLV: среднее > 0 и t > 3 при кластеризации по матчам.
6. Копируемость: край выживает после задержки и издержек.
7. Out-of-sample на новых данных.

Фильтр 4 есть кому проверять: кошелёк `skyman44`
(`0x30c7ac0158499ddc6761047f7f69bcf7d036ac3b`) его ОБЯЗАН НЕ ПРОЙТИ:
его выигрыш $65.8K при полном PnL ~$68.8K, то есть эффективно одна ставка.
Если он проходит — сломан фильтр, а не порог. Порог не крутить.

## Какие файлы зачем нужны

| Файл | Назначение |
|---|---|
| `AGENTS.md` | Правила. Читается всегда. |
| `ORIENTATION.md` | Этот файл. Карта проекта. |
| `PREREGISTRATION.md` | Замороженные критерии. ТОЛЬКО ЧТЕНИЕ. |
| `ASSUMPTIONS.md` | Что проверено, что предположено. |
| `SCHEMAS.md` | Что этапы передают друг другу: колонки, типы. |
| `PROBE_RESULTS.md` | Вердикты Э1-Э4. |
| `PROGRESS.md` | Журнал. Обязателен в конце каждой сессии. |
| `DECISIONS_NEEDED.md` | Вопросы к человеку. Не решать их сам. |
| `TASK*.md` | Текущее задание. |

## Запреты, которые чаще всего нарушают

- Не создавать `pm/broker.py`. Он отсутствует намеренно, чтобы код
  физически не мог поставить ордер.
- Не ставить `py-clob-client` — архивный и нерабочий.
- Не писать числа комиссий нигде, кроме `pm/fees.py`.
- Не считать единицей наблюдения сделку. Единица — матч.
- Не ранжировать по сырому результату. Только по posterior mean со сжатием.
- Не коммитить и не пушить. Коммиты делает человек.
- Не читать и не выводить `.env`.

## Факты инфраструктуры, которые ломают обычные ожидания

- `/orderbook-history` МОЛЧА отдаёт пустоту с 2026-02-20. Пустой ответ —
  не ошибка сети. Исторический стакан существует только тот, что соберём сами.
- CLOB V2 стартует с 2026-04-28. До этой даты другая схема событий.
- Залог — `pUSD`, не `USDC.e`.
- Ончейн-отмены ордера НЕ СУЩЕСТВУЕТ. Значит аварийный стоп — это
  HTTP DELETE плюс проверка `get_open_orders`, а не транзакция.
- Мейкеры комиссию не платят никогда.

## Как заканчивать сессию

Допиши в `PROGRESS.md`: дата, что сделано, что НЕ сделано и почему,
какие файлы тронуты. Если чего-то не смог — это нормальный результат,
молчание — нет.
