# ПОПРАВКА 12 — заморозка правила стороны входа (П11)

- Статус: **заморожено** (два коммита, см. процедуру ниже)
- Область: расчёт `entry_vwap` — сторона входа чистой позиции
- Зависит от:
  - ПОПРАВКА 11 (код) — минимальный дифф в `collect_window_v1.py`
  - ядро-оракул `p11_entry.py` — SHA256 `2eef68c5f5679c8aca27dedc43d852d4347ea6da2bd3f96e75a3390a318f32d1`
  - `collect_window_v1.py` (с П11) — SHA256 `2a2fefbf666abd80618d2ef9100ead585c59912de44f7c4fb3d765af016c9ba1`
- Заморожено коммитом: `__COMMIT_HASH_PLACEHOLDER__`

---

## §1. Факт — асимметрия предиката на границе

Предикат терминальности:

```
is_term_price(p) := (p is not None) and (p >= TERM_HI or p <= TERM_LO)
TERM_HI = 0.999, TERM_LO = 0.001
```

Асимметрия возникает при проверке терминальности на **приведённой** к T\* цене `tp` вместо сырой цены. Для стороны `outcomeIndex == 1` приведение — это комплемент `tp = 1 − p`. На точной границе:

```
1 − 0.999 = 0.0010000000000000009   (IEEE-754 double)
0.0010000000000000009 > 0.001 = TERM_LO
```

Следствие: сделка с **сырой** ценой `0.999` (сторона oi=1) терминальна по сырой цене (`0.999 >= 0.999` → True), но по приведённой `tp = 0.0010000000000000009` — **не** терминальна (`tp <= 0.001` → False, `tp >= 0.999` → False). Симметрично для сырой `0.001` (oi=1): `1 − 0.001 = 0.999`, тут наоборот приведённая попадает в терминальность, а другой край — нет. То есть tp-предикат **пропускает** ровно граничные сырые значения `{0.999, 0.001}` на стороне oi=1.

**Почему самотест на 0.9995 / 0.0005 её не поймал.** Инвариантный тест проверял `is_term_price(p) == is_term_price(1 − p)` на «внутренних» точках. Для `p = 0.9995`: сырая `0.9995 >= 0.999` → True; комплемент `1 − 0.9995 = 0.0005 <= 0.001` → True. Обе стороны True — симметрия соблюдена, ассерт проходит. Дыра проявляется **только** на самих порогах `0.999` / `0.001`, где комплемент из-за округления double приземляется на `0.0010000000000000009`, т.е. на волосок выше порога. Внутренняя точка `0.9995` физически не могла вскрыть краевой зазор округления. Именно поэтому исходный инвариант-цикл падал на `0.999`, и он был заменён на проверку невграничных точек + явные ассерты «дыры».

---

## §2. Правило

1. Терминальность проверяется **один раз**, на **сырой** цене сделки, **до** приведения к T\* (до комплемента `1 − p`).
2. Результат несётся дальше **флагом** вместе со сделкой; повторно на приведённой цене терминальность не вычисляется.
3. Предикат — **дословно** тот же, что в ДОБАВКЕ 3 (фильтр эталона), символ в символ:
   ```
   is_term_price(p) := (p is not None) and (p >= 0.999 or p <= 0.001)
   ```
   применяется к `price = _num(t.get("price"), "price", cond)` — сырому полю сделки.
4. `n_trades` **заморожен**: терминальные сделки остаются в `prematch`, участвуют в `N`, `direction`, `two_sided` и в счётчике `n_trades`. Исключение действует **только** на числитель/знаменатель `entry_vwap` (сторона направления).
5. Новая колонка `n_trades_used_p11` — число сделок стороны направления, реально вошедших в `entry_vwap` после исключения. Пишется только в Проходе B (`--p11`), строго после `n_trades`.
6. Если направление есть (`N != 0`), но после исключения знаменатель пуст (`n_trades_used_p11 == 0`) — пара получает `entry_vwap = None` и **отдельное** `dropped_reason = "p11_terminal_entry_empty"`. Это не ошибка расчёта, а зафиксированный дроп.

---

## §3. Вердикт §3 ПОПРАВКИ 11

**НАРУШЕНИЕ.**

- Доля по окну: **0,0041 %** — 18 пар с пустым входом из 440 848.
- Худший рынок: **wta-hon-grachev-2026-02-13** — **4 из 127 = 3,1496 % > 2 %**.
- Критерий — конъюнкция двух порогов (общий ≤ 0,5 % И по рынку ≤ 2 %). Общий порог удержан (0,0041 % < 0,5 %), но **порог по рынку провален** (3,15 % > 2 %). Конъюнкция провалена ⇒ **окно пересчитывается** (Проход B по всем внутриоконным рынкам).

---

## §4. Регистр тиров

- В замере §3 разбивка по тирам использовала строчные `atp` / `wta`, тогда как данные несут заглавные — из-за рассогласования регистра **разбивка §3 по тирам обесценена** и в вердикте по тирам не используется.
- При этом **общий знаменатель** (440 848) и **максимум по рынку** (4/127) вычислены независимо от регистра тиров и **действительны**.
- Числа **принятого прогона окна** остаются в силе и **пересчёту не подлежат**:
  - валидных пар: **440 848** (ATP **307 151** / WTA **133 697**)
  - строк parquet: **1 266 242**
  - фильтр-1: ATP **529 → 224**, WTA **168 → 95**
- Пересчёт П11 (Проход B) касается **только стороны входа** `entry_vwap` и производной от неё `clv`; перечисленные знаменатели/составы — фиксированы.

---

## §5. Запрещено

1. Менять пороги `TERM_HI = 0.999` / `TERM_LO = 0.001`.
2. Вводить минимальный знаменатель по рынку **задним числом** (post-hoc фильтр на `n_trades_used_p11`).
3. Применять правило §2 к **уже посчитанным** значениям CLV без полного пересчёта (нельзя «поправить» CLV на месте — только пересчёт entry → clv в Проходе B).

---

## Процедура заморозки (два коммита)

1. Коммит с заглушкой: файл содержит строку-заглушку из заголовка «Заморожено коммитом» (ровно одно литеральное вхождение во всём файле).
2. `git rev-parse --short HEAD` — получить короткий хеш этого коммита.
3. Подставить полученный короткий хеш вместо строки-заглушки в заголовке.
4. Коммит подстановки.
5. `git push`.

Проверка единственности заглушки перед шагом 1:

```
grep -c '__COMMIT_[H]ASH_PLACEHOLDER__' POPRAVKA12.md   # должно быть 1; скобка [H] в шаблоне не создаёт второго вхождения токена
```
