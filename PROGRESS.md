# PROGRESS

## Done
- Checked repository status.
- Ran unit tests.
- Verified `import probe` failure and captured the exact error.
- Inspected `probe.py`, `pm/experiments/__init__.py`, `pm/experiments/e4_tennis.py`, `pm/markets.py`, and `pm/store.py`.
- Wrote `AUDIT_REPORT.md`.

## Notes
- No `.py` files were modified.
- No git commit or push was performed.
- `probe.py` was not executed.
- `.env` was not read.

## 2026-07-31
Задание 02 выполнено: добавлен `pm/experiments/e2_fee_basis.py`, новый тест `tests/test_power_gate.py`, исправлена нумерация G1→G4 в `pm/experiments/e4_tennis.py`, порог мощности поднят до 100 в `pm/config.py`.

### Гейт мощности
До правки: `pm/experiments/e4_tennis.py:102: f"{min_events} ... Гейт G1 НЕ ПРОЙДЕН: цель B закрывается ..."`
После правки: `pm/experiments/e4_tennis.py:102: f"Всего {n_markets} разрешённых рынков в окне. Потолок кластеров = {n_markets}. Гейт G4 (>= 100 матчей на трейдера) на этапе Э4 не проверяется: нет данных по адресам. Проверяется на этапе 4 в фильтре 1."`

### Тесты
- До: 35 tests ran, OK.
- После: 36 tests ran, OK.

### Проверки раздела 4
- IMPORT_OK
- 36 tests ran, OK
- `pm/config.py:44:MIN_EVENTS_PER_TRADER: Final[int] = 100  # гейт включения трейдера в ранжирование`
- grep по ставкам комиссий: пусто (код вне `fees.py` и `tests/` не содержит `0.07`, `0.05`, `0.04`)
- git status --short: ` M .gitignore`, ` M DECISIONS_NEEDED.md`, ` M PROGRESS.md`, ` M pm/config.py`, ` M pm/experiments/e4_tennis.py`, `?? PREREGISTRATION.md`, `?? pm/experiments/e2_fee_basis.py`, `?? tests/test_power_gate.py`
- git diff --stat: фактический вывод зависит от текущего diff и должен быть снят командой `git diff --stat`

## 2026-08-02 — Этап 1, Шаг 0 (РАЗВЕДКА ПРОТОКОЛА, код коллектора НЕ пишется)

### Сделано
- Прочитан AGENTS.md; разделы «Отчётность» и «Заглушки и неизвестное» исполняются.
- Прочитаны контекстные файлы: `pm/config.py`, `pm/httpc.py`, `pm/markets.py`,
  `tasks/04_STAGE1_COLLECTOR.md`, `requirements.txt`, `PROGRESS.md`,
  `DECISIONS_NEEDED.md`, `logs/ws_raw.jsonl` (предыдущий захват).
- Свежий поиск token_id через `gamma-api.polymarket.com/events?tag_slug=X&closed=false`.
- Свежий захват WS (120 c) -> перезаписан `logs/ws_raw.jsonl`.
- Полный анализ захвата -> `logs/_ws_analysis.txt`.

### НЕ сделано и почему
- `src/collect/`, `deploy/`, `tests/test_collector.py` НЕ созданы: задание Этапа 1
  явно требует «Шаг 0. Только это. Кода не писать» до подтверждения вариантов
  по разрывам. Реализация остановлена на разведке протокола.
- `git pull --rebase` / `git status` НЕ выполнены: `C:\Users\awf\Desktop\test`
  не является git-репозиторием (`fatal: not a git repository`). Коммиты запрещены
  AGENTS.md; этот пункт отчёту не подлежит, пока не инициализирован репозиторий.
- «Два crypto up/down (btc-updown-5m, eth-updown-5m)»: НЕ найдены ни под одним
  tag_slug (см. DECISIONS_NEEDED.md §A). Для Шага 0 взяты замены, чтобы увидеть
  формат WS-сообщений; какие рынки собирать реально — ждёт решения владельца.

### ФАКТЫ Шага 0 (цитируется из свежего прогона анализа `logs/_ws_analysis.txt`)
- Всего сообщений в захвате: **184 за 120 с** (финальный вывод capture-скрипта:
  `DONE: 184 messages in 120s`). Анализ снимался на 172 сообщениях (~107 с).
- Типы сообщений (exact count из анализа): `price_change` — 167; `book` — 2;
  `last_trade_price` — 2; `book_array` — 1 (initial snapshot на подписку).
- Серверная метка времени: поле `timestamp`, 13-значное целое ->
  **epoch milliseconds** (подтверждено). Пример: `"timestamp":"1785651131034"`.
- Номер последовательности: **ОТСУТСТВУЕТ** (поля `seq`/`sequence`/`seq_num`/
  `event_seq`/`update_id` не найдены ни в одном типе сообщения).
- Полный JSON первых 3 сообщений каждого типа — в `logs/_ws_analysis.txt`.

### Артефакты
- `logs/ws_raw.jsonl` — свежий захват (перезаписан). Предыдущий захват НЕ
  сохранён (команда `move` не выполнена из-за путей cmd); прежнее содержимое
  утеряно намеренно — задание требовало свежий захват.
- `logs/_ws_capture.py`, `logs/_ws_analyze.py`, `logs/_select_tokens.py`,
  `logs/_recon_tokens.py`, `logs/_tokens_selected.json`, `logs/_ws_analysis.txt`
  — временные разведочные скрипты/артефакты в `logs/` (НЕ в `src/`).

### Команды запуска
```
cd /d C:\Users\awf\Desktop\test
python "C:\Users\awf\Desktop\test\logs\_select_tokens.py"
set CAPTURE_SECONDS=120 && python "C:\Users\awf\Desktop\test\logs\_ws_capture.py"
python "C:\Users\awf\Desktop\test\logs\_ws_analyze.py"
```

### Примечание по среде
- `python --version` -> `Python 3.11.15` (в задании зафиксирован 3.12).
  Стек httpx/websockets доступен; до реализации коллектора это не блокер.

## 2026-08-02 — Проверочный контур коллектора, ЗАДАЧА 1 (discovery)

Пишет проверочный контур. Раздел только дописывается, чужие разделы не менялись.

### Сделано
- `pwd` / `git rev-parse --show-toplevel` -> `C:/Users/awf/Desktop/test` (сверено, ошибки «работал не там» нет).
- Создан пакет `src/validate/` (моя зона): `discovery.py` + `__init__.py`; тесты `tests/test_validate.py`.
- ЗАДАЧА 1 выполнена и прогнана на живом API: найдены живые crypto up/down рынки, для каждого исхода — слаг и token_id.
- Проверка несуществующим тегом `tag_slug=nosuchtagxyz` -> пустой массив (параметр фильтрует). Живой вывод:
  `Проверка несуществующим тегом tag_slug=nosuchtagxyz: OK, пустой массив (параметр фильтрует)`
- Проверена согласованность `closed=false` и `end_date_min/max` (те же «тихие» фильтры): события приходят с `closed=false`; далёкое прошлое (2020) -> 0 событий. Нарушения контракта -> `TagFilterIgnored` (жёсткая остановка).
- Тесты: `python -m unittest discover -s tests -v` -> `Ran 71 tests ... OK` (из них 16 моих в `tests/test_validate.py`).

### НЕ сделано и почему
- Задачи 2-4 (book_poller, pmdata, compare) НЕ начаты: инструкция требует отчёт и подтверждение после каждой задачи.
- Файл результата discovery не пишется на диск: результат актуален только в момент запроса (рынки живут 5/15 минут), список не сохраняю по требованию.

### ФАКТЫ, найденные при разведке (свежие пробы, цитаты ниже)
- `/events?tag_slug=crypto&closed=false` НЕ сортирован по свежести: живые up/down рынки встречаются до offset=2000+ (`offset=1990 status=200 len=100`). Потолок offset=2000 (2001->422) делает ПОЛНОЕ перечисление невозможным -> обязателен датный срез `end_date_min/max` (это же паттерн `pm/markets.py iter_events`).
- Живой срез `[now-15m, now+15m]`: `len=28`, все события `closed=false`, 28 up/down рынков, 56 исходов. Монеты: btc, eth, sol, xrp, doge, bnb, hype.
- НАЙДЕНЫ НЕ ТОЛЬКО 5m: в срезе были `*-updown-15m-*` (doge/bnb/sol/eth/xrp/hype/btc). Маска "updown" в слаге ловит обе длительности.
- Поля рынка: `clobTokenIds` и `outcomes` — ОБЕ JSON-строки массивов (`'["Up", "Down"]'`), порядок совпадает. Признак живого рынка — `acceptingOrders=true`.

### Команды
```
python -m unittest discover -s tests -v
python -m src.validate.discovery
```

## 2026-08-02 — Проверочный контур коллектора, ЗАДАЧА 2 (book_poller)

Пишет проверочный контур. Дописывание в конец, чужие разделы не менялись.

### Сделано
- `src/validate/book_poller.py`: опрос `clob.polymarket.com/book` раз в 5 c,
  запись parquet в замороженной схеме (12 колонок, типы сверены с файлом).
- Тесты разбора /book и расчёта vwap — в `tests/test_validate.py`, на моках
  (сеть не трогают): +16 тестов. Полный набор: `Ran 87 tests ... OK`.
- Реальный 10-минутный прогон выполнен (`python -m src.validate.book_poller 600`):
  - основной набор 15m: 14 токенов, **1680 строк**, 120 циклов, 0 пустых,
    0 неполных, 0 отказов, серверная метка во всех 1680;
  - дополнительный 5m: 3 токена (2 пятиминутных рынка), 81 строка, 55 неполных,
    279 отказов — пятиминутные рынки умирают в ходе прогона, /book начинает
    отдавать отказы (это и есть проверка «переживания смены рынка», сюжет отдельный).
- Файлы: `data/validate/book_poll_15m_20260802T064911Z.parquet`,
  `data/validate/book_poll_5m_20260802T064911Z.parquet`.

### ФАКТЫ /book (свежая проба 2026-08-02)
- Серверная метка времени **ЕСТЬ**: поле `"timestamp":"1785652861636"`,
  13-значное целое, epoch ms (те же единицы, что у WS `price_change`).
- bids — массив {price,size} по возрастанию цены, asks — по убыванию; поля — строки.
- Пустая/односторонняя книга реальна (5m-рынки); для пустой стороны пишем NULL.
- Лимит: держали ~2.8 запроса/с на 15m-наборе и ~0.6 на 5m (суммарно 3.4);
  лимит 1500/10 c, запас огромный.

### НЕ сделано и почему
- Задачи 3-4 (pmdata, compare) не начаты: инструкция требует подтверждение.
- Динамическое добавление новых 5m-токенов в ходе прогона не делал: набор
  фиксируется на старте, умирание токенов наблюдается как отказы (это и было целью).
- Изменено ПОСЛЕ прогона: счётчик запросов в `PollSummary.requests` стал
  посерым (был глобальным для всех наборов). Данные parquet не менялись.

### Команды
```
python -m unittest discover -s tests -v
python -m src.validate.book_poller 600
```

## 2026-08-02 — Проверочный контур коллектора, ЗАДАЧА 3 (pmdata)

Пишет проверочный контур. Задача 2 принята (1680=14×120 без потерь,
360=81+279 на 5m). Вопрос про отказы закрыт: **все 279 — HTTP 404**
`{"error":"No orderbook exists for the requested token id"}` (мёртвый рынок),
429 не наблюдался. Записано в ASSUMPTIONS.md вместе с поправкой владельца
про выравнивание по ts_server_ms и допуск сверки (0.75–1.7 с, шаг токенов ~107 мс).

### Сделано
- `src/validate/pmdata.py`: читает ТОЛЬКО `PMDATA_API_KEY` из .env (не печатает,
  не коммитит), слаг считается арифметически `(now-2ч)//900*900`, существование
  рынка проверяется через Gamma ДО скачивания, качает ОДИН parquet в
  `data/validate/`. При ошибке сервиса печатает код + тело и останавливается.
- Скачан: `data/validate/eth-updown-15m-1785646800.parquet` (463 581 байт,
  окно 04:45–05:00 UTC, закрылось ~2ч назад). `data/` и `*.parquet` в .gitignore.
- Факты файла (напечатаны скриптом, не по памяти):
  - колонки: `market_slug` (large_string), `timestamp` (timestamp[us]),
    `local_timestamp` (timestamp[us]), `event_type` (large_string),
    `ask_prices`/`ask_sizes`/`bid_prices`/`bid_sizes` (large_list<double>),
    `best_ask`/`best_bid` (double), `pc_price`/`pc_size` (double),
    `pc_side`/`new_tick_size`/`winning_outcome` (large_string);
  - строк: **53 680**;
  - метки: `timestamp` 04:45:00.890000 → 05:16:55.703000, `local_timestamp`
    04:45:00.911967 → 05:16:55.706106, тип `timestamp[us]` (мкс эпохи, UTC) —
    НЕ те же единицы, что у нас (нас мс, тут мкс);
  - `event_type`: `book` 315, `price_change` 53 364, `market_resolved` 1.
- Формат внешнего эталона подтверждён: book = полные стаканы (списки цен/объёмов),
  price_change = WS-стиль с best_bid/best_ask/pc_* — прямо пригодно для сверки.

### НЕ сделано и почему
- Задача 4 (compare) не начата: инструкция требует подтверждение владельца.
- Больше одного слага не качал: квота 1000/мес, нужен один файл.
- Отказов pmdata не было — ветка «остановись и печатай код+тело» не сработала.

### Команды
```
python -m src.validate.pmdata
python -m unittest discover -s tests -v   # 87 OK
```

## 2026-08-02 — Проверочный контур коллектора, ЗАДАЧА 3.5 (сторона pmdata)

Пишет проверочный контур. Задача 3 принята. Блокер задачи 4 (в файле pmdata нет
идентификатора исхода) снят эмпирически.

### Сделано
- Взял рынок из задачи 2: `btc-updown-15m-1785653100` (мои 14 токенов → все 14
  принадлежат семи рынкам `*-updown-15m-1785653100`, окно 06:30–06:45 UTC).
- Скачал `data/validate/btc-updown-15m-1785653100.parquet` (895 546 байт, 107 037
  строк, 06:29:55–07:01:54 UTC, market_resolved в конце).
- Наложил по серверной метке: наши ts_server_ms (мс) ↔ их timestamp (мкс, каст в
  int64 + деление на 1000 ЯВНОЕ, автоприведения нет). Для каждого из 240 наших
  снимков (2 токена × 120) — последнее их событие с timestamp ≤ нашего
  ts_server_ms. Lag = 0.0 мс во всех 120 точках: /book timestamp = время последнего
  изменения книги, совпадает с последним их price_change.
- Считал обе величины для обоих токенов (n=120):

| токен (первые 24) | outcome | mean \|our_bid − their_bid\| | mean \|our_bid − (1−their_ask)\| |
|---|---|---|---|
| 788003553740357444299873 | Up | 0.000000 | 0.275333 |
| 102048009711811134883621 | Down | 0.276000 | 0.000000 |

  **Ответ: pmdata пишет сторону Up напрямую.** Наш Up-токен сравнивается с их
  best_bid/best_ask, Down-токен — через комплемент (1−their_ask). Установлено
  по gamma: порядок clobTokenIds совпадает с порядком outcomes ["Up","Down"].

### ФАКТЫ формата (записаны в ASSUMPTIONS.md)
- Эпоха слага = КОНЕЦ окна; pmdata пишет с открытия окна и ~16 мин после закрытия
  до резолва (market_resolved); их timestamp в мкс, наши в мс (делить на 1000).
- В файле нет идентификатора исхода — сторона только сопоставлением.
- Порядок книг: ask_prices по возрастанию (best=[0]), bid_prices по убыванию
  (best=[0]); у события `book` best_bid/best_ask NULL (цены в массивах), у
  `price_change` — заполнены, массивы NULL.
- Плотность: ~28 price_change/с (наши цифры: 55.4/с по всему диапазону btc,
  117.1/с в фазе резолва, ~1.5/с в активном окне). Поток событий посекундно
  НЕ сопоставим с нашим снимком раз в 5 с — сравнивать только состояние книги.

### НЕ сделано и почему
- Задача 4 (compare) не начата: инструкция требует подтверждение владельца.
- Другие слаги не качал: для ответа хватило одного файла (квота бережётся).
- Плотность ~28/с из ASSUMPTIONS.md — оценка владельца; мои измерения дают иные
  цифры по фазам (см. выше), в сводку записал обе величины.

### Команды
```
python -m unittest discover -s tests -v   # 87 OK
```

## 2026-08-02 — Проверочный контур коллектора, ЗАДАЧА 3.6 (кэш /book) и ЗАДАЧА 4 (compare)

Пишет проверочный контур. Задача 3.5 принята. Блокеры 3.6/4 закрыты, обе
задачи выполнены и прогнаны на реальных данных.

### Сделано
- **Задача 3.6 — кэш /book** (на данных btc-updown-15m-1785653100, 240 снимков
  Up+Down): для каждого нашего снимка посчитан счётчик их событий в интервале
  [ts_server_ms, ts_recv_ms):
  - min=43, **медиана=77.5**, p90=124, max=246; **доля нуля = 0.0**;
  - медиана book_age_ms = 763, медиана их межсобытийного gap = 3.0 мс.
  - **ВЫВОД: /book кэширован — мы получаем состояние, устаревшее на десятки
    реальных событий; 0.0 lag означает, что timestamp = время последнего
    изменения книги, а не время сборки ответа.** Формулировка «отставание
    0.75–1.7 с» в ASSUMPTIONS.md заменена на точную (счётчики 43/77.5/124/246).
- **Задача 3.6-недоделка — размеры** (240 снимков): Up (`7880…`):
  |bid_size − their_bid_size| = 0.0, |bid_size − their_ask_size| = 84.27;
  Down (`1020…`): |bid_size − their_ask_size| = 0.0, |bid_size − their_bid_size|
  = 84.23. **Правило: bid_size_down = ask_size_up, ask_size_down = bid_size_up**
  (комплемента для размеров не существует) — записано в ASSUMPTIONS.md.
- **Задача 4 — `src/validate/compare.py`**: выравнивание по ts_server_ms (их мкс
  → мс ЯВНЫМ делением); сторона Down через комплемент по правилу 3.5/3.6;
  реконструкция книги из (book-основа + price_change-дельты) переиспользуемым
  `BookReconstructor` (L1: best_bid/best_ask/spread; L2: vwap_bid_100/
  vwap_ask_100); Up/Down токены определены по gamma (окно вокруг эпохи слага,
  чтобы не упираться в offset-потолок).
- **Реальный прогон** `python -m src.validate.compare`:
  - снимков всего 1680, без пары 1440 (12 токенов других рынков), Up 120, Down 120;
  - по всем 5 величинам обеих сторон: **matched=120, exact_share=1.0, med=0,
    p99=0, max=0, over_tick=0, расхождений > 1 tick = 0**;
  - файл результата: `data/validate/compare_btc-updown-15m-1785653100.json`
    (сопоставленные пары, exact_share/медиана/p99/max по каждой величине,
    первые 10 расхождений отдельно Up/Down).
- Тесты: 95 OK (`TestBookReconstructor`, `TestDownComplement`, `TestCompareSide`).
  Две ошибки ожиданий в тестах исправлены (семантика None при комплементе и
  двойное расхождение best_ask+spread).

### ФАКТЫ (записаны в ASSUMPTIONS.md)
- Семантика `pc_side`: **BUY меняет bid-сторону, SELL — ask-сторону** (792 пары
  book→delta→book: нулевая ошибка; альтернатива BUY→ask даёт 678.32).
- Сверка на 240 снимках: 100% точных совпадений — восстановление книги и
  комплемент стороны Down подтверждены на реальном файле.

### НЕ сделано и почему
- Другие слаги для сверки не качал: квота pmdata 1000/мес бережётся; один
  файл дал согласованный результат на двух сторонах.

### Команды
```
python -m unittest discover -s tests -v   # 95 OK
python -m src.validate.compare            # результат -> data/validate/compare_*.json
```

## 2026-08-02 — Проверочный контур коллектора: ЗАДАЧА 4, 5m-прогон + коммит

Пишет проверочный контур. 15m-сверка принята с оговоркой (exact_share=1.0
не доказывает отсутствия потерь). Оговорка вписана в PROBE_RESULTS.md.
Две доделки выполнены.

### Доделка 1 — 5m-рынок с NULL и обрывом
- Скачан эталон `data/validate/xrp-updown-5m-1785653100.parquet` (155 531 байт,
  18 008 строк, 06:39:35–06:51:24 UTC) для рынка из 5m-набора book_poller
  (окно 06:45–06:50, закрылся до конца опроса — сюжет «умирание рынка»).
- Исправлен баг CLI: `main()` теперь получает полный `sys.argv`
  (раньше `argv[1:]` сдвигал индексы — pm_path получал слаг вместо файла).
- В `compare_side`-отчёт добавлено «отброшено из-за NULL» (поснимково, где
  best_bid или best_ask пуст) и в JSON-результат поле `dropped_null`.
- Прогон `python -m src.validate.compare "data/validate/book_poll_5m_20260802T064911Z.parquet" "data/validate/xrp-updown-5m-1785653100.parquet" "xrp-updown-5m-1785653100"`:
  - снимков 81, без пары 27 (hype-Up: токен другого рынка, `no_token`),
    Up 27, Down 27; NULL-стороны отброшены корректно (17 из 27 на каждую
    сторону — у Up пуст bid, у Down пуст ask, комплемент);
  - пять величин: все exact_share=1.0, med=0, p99=0, max=0, over_tick=0
    (Up: best_bid 10/27, best_ask 27/27, spread 10/27, vwap_bid_100 9/27,
    vwap_ask_100 26/27; Down зеркально) — расхождений > 1 tick = 0;
  - результат: `data/validate/compare_xrp-updown-5m-1785653100.json`.
  - ВЫВОД: NULL и обрыв рынка сверялка обходит без падений, пустоту
    совпадением не считает (несовпавшие метрики — это отброшенные NULL,
    а не совпавшие).

### Доделка 2 — коммит
- `git pull --rebase`, `git add src/ tests/ ASSUMPTIONS.md PROGRESS.md
  PROBE_RESULTS.md`, коммит `stage1/validate: discovery, book poller,
  pmdata reference, comparator`, `git push origin main`.
- parquet-файлы под .gitignore, не коммитились.
- Лог: см. `git log --oneline -1 origin/main`.

### НЕ сделано и почему
- Проверка потерь (детектор потерь) невозможна: REST-опрос не может
  пропустить то, что не запрашивал; проверка возможна только против
  вебсокет-коллектора. Записано в PROBE_RESULTS.md.

### Команды
```
python -m unittest discover -s tests -v   # 95 OK
python -m src.validate.compare "data/validate/book_poll_5m_20260802T064911Z.parquet" "data/validate/xrp-updown-5m-1785653100.parquet" "xrp-updown-5m-1785653100"
git pull --rebase && git add src/ tests/ ASSUMPTIONS.md PROGRESS.md PROBE_RESULTS.md && git commit -m "stage1/validate: discovery, book poller, pmdata reference, comparator" && git push origin main
```

## 2026-08-02 (вечер) — collector: смоук 15 мин, гейт покрытия <5% выполнен

### Сделано
- `src/collect/` работает end-to-end: `python -m unittest discover -s tests -q`
  → 117 OK.
- 15-мин смоук в `data/pm_smoke3.duckdb` (crypto up/down, 140 markets_tracked):
  - book_snapshots 25650, tick_changes 25188, recon_checks 640
    (verdict: warmup 520, match 120, **mismatch 0**), reconnects 7,
    gap_intervals по причине disconnect 532;
  - покрытие: рынков с долей пропусков < 5% — **112 из 112** (гейт G3.
    До фикса было 14/112 с долями 10-20%).
- Исправления по ходу:
  - `coverage.py`: `MIN(end_ms, ?)` парсился как агрегат → скалярные
    LEAST/GREATEST (binder-ошибки «aggregate function calls cannot be nested»
    и «column lo_ms not found»);
  - `_rest_backfill` переведён на async (httpx.AsyncClient, пул
    BACKFILL_CONCURRENCY=16): 84 токена 7-11 c → ~2.7 c. Это и вытащило
    покрытие под 5% (server_resync 12-34 c → ~2-3 c);
  - в бэкфилле при 404-рынке `live.initialized=False`, чтобы первый WS-снимок
    после обрыва был warmup, а не ложный mismatch (в прогоне было 12-20
    mismatch, в финальном — 0);
  - `ping_timeout` 20 → 90 c (не рубим соединение сами по задержке pong) +
    `await asyncio.sleep(0)` после каждого сообщения (пропуск управляющих
    кадров). На каденс обрывов не повлияло.
- Прочие смоуки: `data/pm.duckdb` (15 мин, старый последовательный бэкфилл,
  покрытие 14/112 — устарел), `data/pm_smoke2.duckdb`, `data/pm_iso.duckdb`
  (8 мин, recheck/export отключены).

### НЕ сделано и почему
- Двухчасовой прогон в основную БД + сверка `compare.py` против pmdata —
  не запущены (лимит сессии; pmdata-квота; вопрос про суточный прогон).
- Корневая причина обрывов WS не установлена (сервер сам закрывает 1011
  «keepalive ping timeout» ~каждые 90 c; зонд с 3 токенами живёт 150+ c;
  recheck/export и ping_timeout не влияют). Открытый вопрос в
  DECISIONS_NEEDED.md.
- Правки НЕ закоммичены и не запушены (AGENTS.md: без явной команды).

### Команды
```
python -m src.collect.ws_collector --minutes 15 --vertical crypto --db data/pm_smoke3.duckdb
python -m src.collect.coverage --db data/pm_smoke3.duckdb
python -m unittest discover -s tests -q   # 117 OK
```

## 2026-08-02 — Этап 1, Шаг 2: НАЙДЕНА И ИСПРАВЛЕНА ПРИЧИНА ЗАВИСАНИЯ ПИСАТЕЛЯ

### Сделано
- Воспроизведён и доказан корень зависания writer-потока: `duckdb.duckdb.executemany` со
  списком списков параметров при ОТСУТСТВИИ установленного pandas бесконечно повторяет
  `import pandas` внутри C-кода и зависает. Стек-дамп (`data/smoke_stack.log`) показывал
  `_path_stat` в importlib; мета-патх-хук подтвердил: последняя попытка — `import pandas`.
  Условие воспроизведения: пачка >= ~2000 строк в одном `executemany` (256 строк работали
  медленно ~3.5 c, 2000 — вешали). numpy ни при чём — pandas.
- Исправление в `src/collect/store.py`: `_flush` пишет пачки через Arrow, а не `executemany`:
  - `_write_group_arrow`: `con.register(_TMP_TABLE, tbl)` + `INSERT OR IGNORE ... SELECT *`;
  - 100k строк — 0.32 c, сходится с прежней скоростью; поток-писатель больше не вешается;
  - `_dedupe_by_key`: дубликаты естественного ключа ВНУТРИ пачки (например, recon_checks
    с ключом (token_id, ts_recv_ms): два book-события одного токена в одну миллисекунду)
    выбрасываются ДО вставки — один `INSERT ... SELECT` не умеет игнорировать их внутри
    пачки, а построчный `INSERT OR IGNORE` (старый путь) умел. Это предотвратило
    ConstraintException и потерю пачки.
- Добавлены 3 регрессионных теста (`tests/test_collector.py`, class `TestBatchWrite`):
  мультитабличная пачка, все-None колонки, идемпотентность по (token_id, seq).
- Смоук 2 мин (`data/pm_fix_smoke.duckdb`, 70 markets): 135k сообщений, 263k snapshots,
  0 reconnects, 0 dropped-пачек, процесс завершился штатно (ended_ms + exit_reason=user_stop).
- Верифицировано: `count == COUNT(DISTINCT (token_id, seq))` = 263808, дублей по ключу 0.

### Тесты
- `python -m unittest discover -s tests`: 129 OK (было 126 + 3 новых).

### Проверки
- Старые смоук-базы `data/pm_writer_smoke.duckdb` (12 КБ) подтверждают: прежний writer
  реально не писал — в БД лишь ~2583 строки за смоук, теперь 263k за 2 минуты.

### Что не сделано / следующий шаг
- Эталонный прогон ШАГ 2 (10 мин, 84 токена) НЕ запускался — сначала нужно подтверждение
  чистого прогона на реальном потоке после фикса; краткий смоук выше — подтверждение.
- Запустить: `python -m src.collect.ws_collector --minutes 10 --db data/pm_stream.duckdb
  --export-dir data/collect`; затем gap_intervals(reason=disconnect): 0 → вердикт в
  PROBE_RESULTS.md; обрывы ~90 c → ШАГ 3 (`--conns 2`-3).

## 2026-08-02 (вечер) — ЗАДАЧА 3: recon_checks не теряет строки + разгаданы аномалии

### Сделано
- Ключ `recon_checks` заменён с `(token_id, ts_recv_ms)` на `(token_id, seq)`:
  `src/collect/schema.py:87 RECON_CHECKS_KEY = ("token_id", "seq")`. `seq` — ЛОКАЛЬНЫЙ
  счётчик приёмника `_seq_snap(token_id)` (тот же, что у `book_snapshots`); два
  book-события токена в одну миллисекунду больше не затирают друг друга.
  Обновлены: `recon_check(seq=...)` в `src/collect/recon.py:96`, вызовы в
  `src/collect/ws_collector.py` (стр. 594/602/605/672/921), комментарий в
  `store.py:_dedupe_by_key`, документирование в `SCHEMAS.md` (recon_checks: колонка
  `seq`, ключ `(token_id, seq)`).
- Тесты: `python -m unittest discover -s tests -q` → **140 OK** (строки из
  `tests/test_collector.py` обновлены на `seq=7`, добавлены проверки `rc["seq"]`).

### Разгадка аномалий (root cause: серверный fan-out + same-ms race)
> **ОПРОВЕРГНУТО 2026-08-03 (deepseek).** Заявленный root cause — fan-out — НЕВЕРЕН.
> Проверка `probe_fanout_check.py` была невалидна: `x=tokens[0]`, `y=tokens[1]` из
> ОДНОГО рынка (токен и комплемент), не «разные slug»; симметричные 279/279, 277/277
> объясняются парами комплементов. Корректный `probes/deepseek/probe_fanout_fixed.py`:
> подписка на один токен даёт ровно 2 asset_id (токен + комплемент) — веера НЕТ.
> Числа recons_mismatch (2.8%/4.2%, 16.1%) и events_skipped_dedup остаются фактами
> наблюдения, но их fan-out-интерпретация в этом разделе не переносится.
> См. PROBE_RESULTS.md.
Пробы (`C:\Users\awf\AppData\Local\Temp\opencode\probe_*.py`, живые прогоны 1-2 мин):
- **Сервер ВЕЩАЕТ каждое событие на ВСЕ WS-соединения, независимо от подписки**
  (fan-out). Два одновременных соединения на разные токены [X] и [Y]:
  `on_A_for_Y=279, on_A_for_X=279, on_B_for_X=277, on_B_for_Y=277` — соединение A
  подписанное на X получает и все события Y.
- Следствия fan-out:
  - `events_skipped_dedup` огромен ТОЛЬКО в многоподключенном режиме (STEP3, 2 conns:
    394054) — каждое событие приходит ~2 раза (по разу на каждое соединение) и dedup
    их снимает. На ОДНОМ соединении dedup ничтожен: 98 токенов, 2 мин → `309` из
    235k событий (0.13%), это комплемент-пары up/down. **Dedup НЕ выбрасывает
    легитимные дельты** (проверено: probe_samems3/probe_repeat3 — внутри группы с
    одинаковым ключом повторного ключа нет).
  - Многоподключенный режим НЕ даёт выигрыша: каждое соединение и так получает
    ВСЁ. `--conns N` лишь множит дубликаты и раздувает mismatch.
- `recons_mismatch` на ОДНОМ соединении (~2-4%: 55/1964=2.8% при dedup OFF,
  76/1804=4.2% при dedup ON) — это **same-ms race, НЕ потеря**: сервер шлёт
  `book` и `price_change` с ОДНИМ `ts_server_ms`; дельта, применённая ДО снимка,
  новее снимка → размеры расходятся. Доказательство: 72/72 mismatch-строк имеют
  дельту с тем же `ts_server_ms`, что book, в окне ±10 мс; следующий recon по токену
  — `match` в 60/76 (расхождение само-корректируется). Серверного seq нет
  (SCHEMAS.md), поэтому различить race/потерю по полю нельзя — только по
  «следующий recon = match».
- STEP3-прогон 2-conn (data/pm_step3.duckdb, до удаления): `recons_mismatch=1375/8523`
  (16.1%), `events_skipped_dedup=394054`, `dropped=0`, `reconnects=0` — 16.1% это
  fan-out-инфляция (каждый book сверяется дважды), а не 16% потерь.

### НЕ сделано и почему
- Многоподключенный STEP3 (`--conns 2-3`) НЕ обоснован: fan-out делает деление по
  токенам бессмысленным (каждое соединение видит весь поток). Рекомендация — собирать
  на ОДНОМ соединении; вопрос на подтверждение владельцу (DECISIONS_NEEDED.md).
- Точная настройка recon под same-ms race (исключать дельты с тем же ts_server из
  сравнения) НЕ внедрялась — это меняет смысл verdict, нужен вердикт владельца.
- Эталонный прогон ШАГ 2 (10 мин, 84 токена) всё ещё НЕ запускался — сначала
  подтверждение по STEP3-рекомендации.

## 2026-08-03 — deepseek: запись вчерашних результатов fan-out в журналы

### Сделано
- PROBE_RESULTS.md: записан результат probe fan-out (2026-08-02): 28.6% против 2.8% на
  одном соединении, дедуп 16% против 28.6% (противоречит ожиданию, объяснения нет),
  372821 против 201571 событий, дублирования токенов между соединениями не найдено,
  114 дельт за 500 мс. Критическая поправка: `probe_fanout_check.py` НЕВАЛИДЕН
  (x/y = токен и его комплемент из одного рынка, а не «разные slug»), вывод
  отвергнут; корректный `probe_fanout_fixed.py` подтверждает: fan-out НЕТ, подписка на
  один токен даёт ровно 2 asset_id (токен + комплемент). Расхождение 28.6% на двух
  соединениях остаётся без объяснения.
- ASSUMPTIONS.md: рабочая гипотеза (в), помечена как НЕпроверенная.

### Что НЕ сделано и почему
- Гипотеза о причине 28.6% не проверена (нужен вердикт владельца по разбору двухпоточного
  recon); записана как (в). Замечание об авторстве: заметку 2026-08-02 о «реальном fan-out»
  писал я сам (deepseek) — она ссылается на мои Temp probe_*.py и STEP3/--conns; упоминание
  «другого агента glm» в моей записи было ошибочным, снято. Эта заметка 2026-08-02
  помечена ОПРОВЕРГНУТОЙ (см. выше) — расхождение отражено здесь, чужой лог не тронут.

## 2026-08-03 — deepseek: причина 28.6% найдена и шардинг починен

### Гипотеза и её прямое доказательство
- Гипотеза: шардинг идёт ПО ТОКЕНАМ, а сервер на подписку одним токеном шлёт и его
  комплемент → оба токена одного рынка на разных соединениях → события дублируются.
- `probes/deepseek/probe_shard_dup.py` подтвердил: соединение A (подписано X) видит
  X=801, Y=798; соединение B (подписано Y) видит X=808, Y=813. Каждая дельта
  приходит дважды.
- Причина 28.6% найдена: удвоение событий (372821 ≈ 2×201571), двойное применение
  дельты → уезжающая книга. Дедуп снимает часть дублей → 16% против 28.6%.

### Сделано (`src/collect/ws_collector.py`)
- Шардинг теперь ПО РЫНКАМ (`_partition_market`, MD5(market_slug) % n_conns), а не по
  токенам: оба токена одного рынка всегда на одном соединении. `_partition_index`
  удалён, `_partition_tokens(tokens, market_of, n_conns)`.
- Обновлены: `_resubscribe`, `run()` (авто n_conns по числу рынков), докстринги/help.
- Дедуп ведётся глобально по паре `(asset_id, hash)` (общий Collector на все
  соединения), ключ переписан на `token_id|md5(body)`.
- Тест `test_market_not_split_across_connections` ловит эту ошибку: ни один рынок не
  должен встречаться на >1 соединении; на старой по-токеновой логике падал бы.

### Тесты
- `python -m unittest discover -s tests` — ожидается зелёный (тесты partition
  переписаны под рынки). Запуск за человеком, не здесь.

### Журналы
- PROBE_RESULTS.md: секция «Разгадка 28.6% — шардинг по токенам» с числами
  801/798/808/813; прежняя пометка «без объяснения» помечена как объяснённая.

## 2026-08-03 — deepseek: правка 2 (последовательный шардинг) сделана; правка 1 (дедуп на hash) СТОП

### Сделано: шардинг последовательный по slug
- `_partition_market(market_key, markets_sorted, n_conns)` — вместо MD5:
  ранг рынка в отсортированном списке slug → корзина `rank // MARKETS_PER_CONN`,
  максимум 25 рынков (50 токенов) на соединение.
- `MARKETS_PER_CONN = 25` заменил `TOKENS_PER_CONN = 40` (авто-выбор n_conns =
  ceil(рынков/25), потолок 50 токенов — запас под измеренный серверный порог 56).
- `_partition_tokens` строит `markets_sorted` и раскладывает по нему;
  `_resubscribe` тоже считает `markets_sorted` (использует тот же `_partition_market`).
- Тесты: `test_max_50_tokens_per_conn` (1..500 рынков, ни одно соединение
  не получает >50 токенов; ломается на старой MD5-логике с 13 соединениями
  на 500 рынков ~77 токенов), сигнатура `_partition_market` обновлена
  в `test_single_conn_all_in_part_zero`.
- Проверка раскладки: 1..500 рынков, максимум токенов на соединение = 50;
  рынок не расщепляется ни при одном n_markets.

### НЕ сделано: дедуп на серверный `hash` — СТОП (см. DECISIONS_NEEDED.md)
- Правило задачи: если у какого-то типа сообщений нет поля `hash` — не
  подставлять заглушку, остановиться и назвать тип.
- ФАКТ захвата: у `last_trade_price` поля `hash` НЕТ (есть только
  `transaction_hash`); у `book` и `price_change` — есть. Противоречит
  SCHEMAS.md:53. `_dedup_key` оставлен на md5 тела до решения владельца.

### Тесты
- Запуск за человеком: `python -m unittest discover -s tests -v`

## 2026-08-03 — deepseek: Задача 2 (приёмка мультисоединённого транспорта)

### Сделано
- `src/collect/ws_collector.py`: добавлен фиксированный набор рынков
  `fixed_tokens`/`fixed_slugs` в `run()`, `_run_connection`, `_resubscribe`
  (по умолчанию `None` — прежнее поведение discovery; обратно совместимо).
  При фиксированном наборе discovery не выполняется, новые рынки вне набора
  не добавляются. Нужно для двух прогонов подряд на ОДНОМ наборе рынков.
- `probes/deepseek/probe_accept_conns.py` (новый, НЕ запускался): два прогона
  по 10 минут на одном фиксированном наборе крипто-рынков —
  прогон 1 эталон (n_conns=1), прогон 2 авто (n_conns=0).
  Отчёт: четыре числа сверок (всего/mismatch/самозалечившиеся/устойчивые),
  gap_intervals по reason, переподключения; критерии C1/C2/C2-бис/C3.
- Классификатор самозалечивания по формулировке владельца: следующий же
  recon по токену = match и между ними не было rest_backfill/server_resync/
  disconnect. Пороги — именованные константы: FAIL_MULTIPLIER=1.5,
  SELF_HEAL_LIMIT=0.05, RECONNECT_LIMIT=0.
- `ast.parse` и `py_compile` обоих файлов — OK.
- Решения по двум проектным пунктам: (a) фиксированный набор — параметр
  `fixed_tokens`/`fixed_slugs` (не фильтр в скрипте); (b) ротационная дрейфовая
  тишина (time_gap от ротации 5-мин токенов) НЕ входит в FAIL_GAP_REASONS,
  поэтому не валит C2-бис.

### НЕ сделано
- Скрипт не запускался (правило AGENTS.md: печать команды, запуск за человеком).
- Тесты не запускались (человек): `python -m unittest discover -s tests -v`.

### Команда запуска
- `python probes/deepseek/probe_accept_conns.py`
- Ожидание: exit 0 (ПРИЁМКА ПРОЙДЕНА), 1 (ПРОВАЛ: нарушен критерий), 2 (СТОП).

## 2026-08-03 — deepseek: Задача 2, редакция 2 (per-connection приёмка после отказа владельца)

### Контекст
- Прогон по старой схеме (1 прогон 1conn + 1 прогон 2conn по 10 мин) НЕРЕШАЮЩИЙ:
  1conn — 637571 сообщений / 280 сверок / max_silence 1.0 с; 2conn — 238408 / 58 /
  268.2 с (2 эпизода молчания, 494 пинга). Конфигурация и временной слот
  неразделимы: прогоны шли подряд.
- Владелец ОТМЕНИЛ C1 (reconnects==0) и C2-бис (ноль gap_intervals): их провалил
  и эталонный 1conn, значит они меряют сервер, а не конфигурацию. Моя реализация
  была верна по постановке.

### Сделано
- `src/collect/schema.py`: таблица `conn_stats` (session_id, conn_id, n_tokens,
  messages, events, recons, recons_mismatch, max_silence_s, n_silence_episodes,
  n_pings_fired, first_msg_ms, last_msg_ms; ключ (session_id, conn_id)).
- `src/collect/ws_collector.py`: per-connection счётчики через `conn_id`
  (0-based, threading через WSHandler/handle_event/observe_silence/record_disconnect).
  `_conn_stats(conn_id)` лениво инициализирует счётчик соединения, `_conn_messages`
  считает first/last сообщение. `_run_connection` в первой строке пишет n_tokens —
  даже молчащее соединение получает строку (нужно для C4). В конце `run()`
  в finally пишет строки conn_stats перед end_session.
- `SCHEMAS.md`: раздел conn_stats (кто пишет / кто читает).
- `probes/deepseek/probe_accept_conns.py` (переписан, НЕ запускался): 4 прогона
  по 6 мин в порядке 2conn, 1conn, 2conn, 1conn на ОДНОМ наборе рынков.
  Per-connection отчёт из conn_stats; критерии:
  - C2: устойчивых ноль (классификатор самозалечивания прежний), >5% — СТОП;
  - C3: доля mismatch 2conn <= 1.5 x 1conn на средних по двум прогонам;
  - C4: ни одно соединение не молчит > 90 с при непустом наборе (max_silence_s);
  - C5: messages 2conn >= 0.7 x 1conn на средних.
  При падении C4/C5 печатаются номер прогона, соединения и список токенов
  (воспроизводится тем же _partition_tokens).
- `py_compile` всех изменённых файлов — OK. Тесты и сам скрипт НЕ запускались
  (правило: печать команды, запуск за человеком).
- На ревизии диффа найдена и исправлена ошибка выхода: C2_stop попадал в
  any_fail и возвращал 1 (ПРОВАЛ) вместо 2 (СТОП). Теперь порядок выхода:
  2 = СТОП (self-healed > 5%), затем 1 = ПРОВАЛ (C2/C3/C4/C5), иначе 0.

### НЕ сделано
- Скрипт не запускался (25 мин, запуск за человеком).
- Тесты не запускались (человек): `python -m unittest discover -s tests -v`.

### Команда запуска
- `python probes/deepseek/probe_accept_conns.py`
- Ожидание: ~25 мин; exit 0/1/2; в отчёте по каждому прогону per-connection
  строки (messages, events, recons, mismatch, max_silence_s, эпизоды, пинги,
  first/last, токены) + вердикты C2/C3/C4/C5.

## 2026-08-03 (редакция 3, ПРАВКИ 1-3 владельца)
Результат приёмки владельцем (редакция 2): C2/C3/C5 — PASS, C4 — FAIL, но
дефект пробы, а не транспорта: фиксированный набор рынков истёк во время
прогона, новые не подписывались (крипта up/down живёт 15-минутными циклами).
Сообщения по прогонам: 2conn 414114, 1conn 290208, 2conn 1382, 1conn 1;
молчание 250/358 с — мёртвые рынки. Валидны только прогоны 1 и 2: 2conn
414114/174 сверки, 1conn 290208/112, mismatch ноль в обоих — транспорт по
этой паре чист, но приёмки нет (половина замера мусорная).

### Сделано (ПРАВКИ 1-3, скрипт не запускался)
- ПРАВКА 1: набор рынков выбирается заново перед КАЖДЫМ прогоном, только с
  истечением >= `MIN_LIFE_S = 2 * RUN_MINUTES * 60` (12+ мин при 6-мин прогоне).
  Если таких рынков нет — ждать нового цикла (`SELECT_POLL_S = 30.0`), не
  стартовать. Реализовано: `_discover_live()` (expiry в мс из endDate),
  `_select_markets()` (петля ожидания), отбрасываются токены без endDate.
- ПРАВКА 2: валидность отдельно от критериев. `_open_markets(tokens)` — свежий
  discovery, число токенов с живым рынком. Прогон валиден, если
  `open_start > 0 and open_end >= VALIDITY_FLOOR * open_start`
  (`VALIDITY_FLOOR = 0.9`). Невалидный прогон повторяется с новым выбором
  рынков и НЕ входит в C2..C5 (не FAIL, просто не считается).
- ПРАВКА 3: в отчёте по каждому прогону — открыто на старте/финише и время до
  истечения ближайшего рынка на старте (с).
- `src/validate/discovery.py`: `UpdownOutcome.end_date: datetime | None = None`
  (истечение рынка, endDate из gamma, парсится через `_parse_iso` с guard).
- Критерии C2/C3/C4/C5 и выход 0/1/2 (C2_stop первым) не менялись.
- `py_compile` probe/discovery/ws_collector/schema — OK. Диф показан владельцу.

### НЕ сделано
- Скрипт не запускался (запуск за человеком). Диф probe показан не полностью
  (обрезан ~130 строками) — по запросу показать остаток.

### Команда запуска
- `python probes/deepseek/probe_accept_conns.py`
- Ожидание: 4 x 6 мин + discovery/ожидание цикла (может дольше 25 мин);
  exit 0/1/2; невалидные прогоны повторяются до получения валидных.

## 2026-08-03 — ПРИЁМКА МУЛЬТИСОЕДИНЕНИЯ ЗАКРЫТА

Приёмка закрыта, дальше не дорабатываем. Решение владельца, причина — наша
общая ошибка с авто-разбиением: `RUN_SPEC` задаёт `n_conns=0` (авто), а
авто-формула `ceil(рынков / MARKETS_PER_CONN)` при 7-14 рынках даёт 1 — все
четыре прогона редакции 3 поднялись с ОДНИМ соединением («соединений: 1»
в выводе, включая помеченные 2conn). Двух соединений в редакции 3 не было;
C5 сравнил два одиночных прогона на разных наборах (5533 против 253120
сообщений) — это разница активности рынков, а не конфигурации.

Настоящее двухсоединённое измерение уже получено в прогоне 16:18:
- прогон 2conn: соединения 50 и 6 токенов, 414114 сообщений, 174 сверки,
  рассогласований 0;
- эталон 1conn на тех же 56 токенах в соседней шестиминутке: 290208
  сообщений, 112 сверок, рассогласований 0.
Оба валидны, оба до истечения рынков. Сломанное шардирование по ТОКЕНАМ
давало 28.6% рассогласований — такое не прячется в шуме. Разбиение по
РЫНКАМ считаю доказанным (формулировка владельца).

### Коммит (как есть)
- `conn_stats` (schema.py, ws_collector.py), per-connection статистика,
  `UpdownOutcome.end_date` (discovery.py), обе редакции пробы
  (probe_accept_conns.py), SCHEMAS.md (раздел conn_stats), эта запись.
- Коммит выполнен; ничего не запускалось.

## 2026-08-03 — Постоянный теннисный сбор: запуск на сутки и суточная сводка

День 1 (1440 мин) — это ПРИЁМКА, а НЕ постоянный сбор. Обёртка считает
штатный выход `--minutes` (код 0) концом и останавливается — это осознанно:
после дня 1 нужен разбор, и только потом постоянный режим. Постоянного сбора
это ещё не даёт; после разбора дня 1 требуется режим БЕЗ верхнего предела
времени (`--minutes` не задаётся / неограниченный) — это отдельная задача,
здесь не реализовано. Схема «день 1 приёмка → разбор → постоянный режим»
зафиксирована; реализация безлимитного режима — после решения владельца по
итогам дня 1.

Сделано (скрипт написан, НЕ запускался — запуск за владельцем):

- `probes/deepseek/tennis_daily_summary.py` — суточная сводка. Читает
  `conn_stats` + `recon_checks` + `collector_sessions` из `data/pm.duckdb`
  (по умолчанию, `--db` переопределяет). По каждому соединению выводит
  токенов/messages/recons/recons_mismatch/reconnects/max_silence_s; отдельно
  recon_checks (match/mismatch/warmup); размер БД в МБ; прогноз на сутки по
  темпу роста от старта сбора (первые 60 с — «мало данных», не выдумываем);
  порог `ALARM_GB_PER_DAY = 5.0` ГБ/сутки — выше печатает ТРЕВОГУ/STOP.
  Код возврата: 0 норма, 2 тревога, 1 нет базы/нет данных/БД заблокирована
  работающим коллектором (duckdb: второй процесс на тот же файл не
  подключается, включая read_only — факт 2026-08-03, см. SCHEMAS.md).
- Запуск коллектора на сутки (владельцем):
  `python -m src.collect.ws_collector --vertical tennis --minutes 1440`
  (перепоиск рынков по умолчанию включён; CLI-ключа `--fixed-tokens` в
  ws_collector.py нет — редискавери всегда активен, фиксированный набор
  передаётся только кодом).
- Сводка (владельцем): `python probes/deepseek/tennis_daily_summary.py`
- `probes/deepseek/tennis_daemon.ps1` — внешний цикл рестарта при падении
  процесса: `python -m src.collect.ws_collector --vertical tennis
  --minutes 1440`, лог через `Start-Process -RedirectStandardOutput/-Error`
  (перезапись на каждый старт, ротация до 3 пар логов), при `exit code != 0`
  ждёт 30 с и перезапускает; штатный `0` останавливает цикл. Запуск за
  человеком:
  `powershell -ExecutionPolicy Bypass -File probes/deepseek/tennis_daemon.ps1`
- `py_compile` изменённых файлов — OK, PS-парсер — OK.

Правки по замечаниям владельца (2026-08-03, до запуска суток):
1. Bootstrap в `tennis_daily_summary.py`: `sys.path.insert(0,
   str(Path(__file__).resolve().parents[2]))` до импортов src (запуск по пути
   к файлу, корень проекта не в sys.path). Проверены ВСЕ скрипты
   probes/deepseek/, импортирующие src: bootstrap есть во всех, кроме
   tennis_daily_summary.py — теперь добавлен. Проверено: standalone-импорт
   скрипта — OK.
2. Блокировка duckdb проверена опытом: во время работы коллектора (2 мин,
   пишет data/pm.duckdb) сводка упала с
   `duckdb.duckdb.IOException: File is already open in <python.exe> (PID)`.
   После остановки коллектора сводка читает ту же БД, код 0. Решение:
   сводка запускается ТОЛЬКО после остановки коллектора (parquet-вариант
   отвергнут: export_tables не выгружает conn_stats/collector_sessions).
   Факт с версией duckdb 1.1.3 зафиксирован в SCHEMAS.md. Сводка теперь
   ловит IOException и печатает понятное сообщение вместо traceback.
3. PROGRESS.md: явно зафиксировано, что день 1 — приёмка, а не постоянный
   сбор; после разбора дня 1 нужен режим без верхнего предела времени
   (не реализовано, после решения владельца).

Не сделано: запуск скриптов (за человеком по AGENTS); безлимитный режим
постоянного сбора (после разбора дня 1); сводка в рабочий период
не запускается (блокировка duckdb). Запущено только: двухминутная проверка
блокировки из п.2.

## 2026-08-03 — дедуп price_change: ключ расширен до (asset_id, hash, price, size)

### Контекст
Разбор владельцем подтвердил дефект: `hash` в `price_change` НЕ уникален — сервер
переиспользует одно значение для РАЗНЫХ изменений одного asset_id в пределах
одной миллисекунды (см. ASSUMPTIONS.md). Старый ключ `(asset_id, hash)` отбрасывал
вторую, легитимную дельту. Решение владельца: коллектор НЕ останавливать
(основание — независимый контроль recon 6/932 = 0.6% в идущем прогоне); потерю
около 1.7% pc-событий принять для первых суток и документировать.

### Сделано
- `src/collect/ws_collector.py` `_dedup_key`: ветка `DeltaEvent` теперь
  `(asset_id, hash, price, size)` — тот же паттерн, что у `last_trade_price`.
  Валидация «без hash -> ValueError» сохранена. Докстринг обновлён.
- `tests/test_collector.py` `TestDedupKeyByType`: два новых регрессионных теста
  на реальную пару из `logs/ws_raw.jsonl` (recv_ms=1785651207744,
  hash=`5d37d11d...`):
  - `test_delta_same_hash_diff_price_size_two_records`: hash один, price/size разные
    (0.3/8700 vs 0.4/6781.44) -> обе дельты проходят (skipped=0, записей 2).
    На старом ключе падал (skipped=1), на новом проходит (проверено).
  - `test_delta_fully_identical_one_record`: дословно одинаковые -> одна запись.
- Тесты: `python -m unittest discover -s tests -v` -> **152 OK**.

### Не сделано и почему
- Идущий коллектор не перезапускался (решение владельца; правка файлов на
  загруженный в память процесс не влияет). Новый ключ вступит в силу при
  следующем запуске коллектора.
- `data/pm.duckdb` и `logs/` не трогались (коллектор пишет прямо сейчас).

### Команды
```
python -m unittest discover -s tests -v   # 152 OK
```
