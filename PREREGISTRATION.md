# PREREGISTRATION.md

## 1. Правило заморозки
Дата и время заморозки (MSK): 2026-07-31 14:10 MSK.
Коммит-хеш: `3272fa3`.

После заморозки пороги, критерии и определения исходов не меняются. Любое изменение оформляется как пронумерованная поправка в секции ПОПРАВКИ, с датой, причиной и явным указанием, были ли на момент поправки видны результаты.

## 2. Цели
| Цель | Суть | Априорная вероятность успеха | Что считается успехом |
|---|---|---:|---|
| A | Свой терминал вместо платной подписки | 95% | Рабочий личный аналитический слой над Polymarket |
| B | Проверить, есть ли у теннисных трейдеров копируемый edge | 12% | GO по зафиксированному критерию |
| C | Опубликовать датасет по чистоте рынка | 70% | Выпуск открытого market-integrity dataset |

NO-GO по цели B — нормальный и ожидаемый исход, цель A от него не зависит.

## 3. Гипотезы
H0: у отобранной популяции теннисных трейдеров средний CLV после издержек не отличается от нуля (`gross_delta - cost <= 0`).

H1: `gross_delta - cost > 0` устойчиво к неопределённости базы комиссии и к кластеризации.

Тест односторонний.

## 4. Единица наблюдения
Матч, не сделка. Минимум 100 матчей и 60 дней на адрес. Одно и то же число 100 используется и в фильтре 1, и в гейте мощности.

## 5. Определения
- `gross_delta` — средняя разница между ценой входа трейдера и ценой закрытия рынка (CLV) в долях цены, агрегированная по матчам.
- `cost` — точечная оценка издержек: комиссия тейкера по `pm/fees.py` плюс спред-издержка.
- `bracket_width` — ширина интервала издержек между гипотезами базы комиссии (`shares` против `notional`), `FeeQuote.bracket_width`.
- `SE_edge`, `SE_cost` — кластерные стандартные ошибки.

## 6. Критерий GO

```
gross_delta - cost > 2.5 * sqrt(SE_edge^2 + SE_cost^2)
```

K = 2.5. Под корнем СУММА квадратов, не максимум.
Множественность — Benjamini-Hochberg, q = 0.10.

## 7. Исходы, фиксированный порядок проверки
1) UNDECIDABLE, если `bracket_width > gross_delta`;
2) GO, если выполнен критерий выше;
3) NO-GO во всех остальных случаях.

Порядок не переставляется ни при каких результатах.

## 8. Четыре гейта (конъюнкция)
- G1 — отображаемая цена: mid или last trade (Э1)
- G2 — база комиссии `C` в `fee = C × feeRate × p × (1−p)`: shares или notional (Э2)
- G3 — наличие собственных снимков стакана за нужный период
- G4 — мощность: достаточно ли данных, чтобы различить гипотезы.
  Порог — не менее 100 матчей НА ТРЕЙДЕРА, то же число, что в фильтре 1. Сравнение идёт по числу матчей конкретного адреса, НЕ по общему числу событий на рынке.

GO невозможен, пока не закрыты все четыре. Порядок закрытия любой.

## 9. Семь фильтров, порядок фиксирован
0 — популяция целиком, не лидерборд;
1 — ≥100 матчей и ≥60 дней;
2 — детекторы integrity;
3 — зомби-позиции;
4 — концентрация `max_market_pnl / total_pnl < 0.25`;
5 — CLV: среднее > 0, t > 3 с кластеризацией по матчу;
6 — копируемость;
7 — out-of-sample.

Контрольные тест-кейсы: `skyman44` (`0x30c7ac0158499ddc6761047f7f69bcf7d036ac3b`) обязан провалить фильтр 4; `flatbarrel` (2448 предсказаний за ≈90 дней) не должен быть срезан.

## 10. Кластеризация
Два уровня: матч и турнирный день. Считаются оба, побеждает консервативный вариант (больший SE).

## 11. Запрещённые действия при чтении результатов
- менять K;
- менять q;
- менять порог 100;
- менять порядок исходов;
- менять порядок фильтров;
- исключать адреса post hoc;
- выбирать окно данных после просмотра результата;
- заменять кластеризацию на менее консервативную;
- трактовать UNDECIDABLE как NO-GO или как GO;
- агрегировать по сделкам вместо матчей;
- брать лидерборд вместо полной популяции;
- использовать коммит-хеш прошлого состояния вместо фактического HEAD.

## 12. Расхождения кода и предрегистрации на момент заморозки
| Что | Код | Предрегистрация |
|---|---:|---:|
| `MIN_EVENTS_PER_TRADER` | 30 | 100 |
| Гейт мощности | сравнивает события рынка, а не трейдера | сравнивает матчи конкретного адреса |
| Нумерация G1/G4 | сломана в трёх местах | единая нумерация G1–G4 |

Пометка: устраняется заданием 02.

## 13. ПОПРАВКИ
- Поправка 7 (2026-08-02): предматчевая закрывающая линия как измеряемая величина; см. POPRAVKA7.md. Хэш заморозки: 7b65c2a.
- Поправка 5 (2026-08-01): ОТМЕНЕНА 2026-08-02 Поправкой 7 (измеряла реализованный PnL, а не CLV).

### Поправка 1 — 2026-07-31, коммит 011c324

Метод Э1 через сумму дополнительных ног YES и NO признан
недействительным. Основание: прогон дал pooled_sigma_sum = 0.0 и
max_abs_dev = 0.0 на всех 35 рынках с данными, что доказывает
арифметическое вычисление ноги NO как 1 - YES на стороне сервера.
Сумма равна единице по построению и не несёт информации о базе цены.

Замена: сравнение значения prices-history с одномоментным снимком
стакана /book того же токена.

Пороги, правило решения GO, K = 2.5, q = 0.10 и порядок проверки
исходов не изменяются. Поправка внесена до получения любого вывода
по гипотезе B.

### Уточнение 1 — 2026-07-31, коммит 011c324, до получения любых значений CLV

Единица наблюдения «матч» в строке 26 означает одиночный матч.
Парный теннис (слаги atp-doubles-*, wta-doubles-*) из основного
анализа исключается: ликвидность и качество закрывающей линии
там принципиально иные.

Парные матчи допускается анализировать как разведочную ветку.
Её результат НЕ участвует в решении GO/NO-GO, не входит в
FDR-семейство и не может быть повышен до основного после
просмотра данных.

Пороги, правило GO, K = 2.5, q = 0.10 и порядок исходов
не изменяются.



# ПОПРАВКА 2 к предрегистрации Э1 (капчер живого рынка → качение)

Статус: заморожена коммитом 643cdc9f7fa221dfab4cd6b471afe5efd3795dcf
Дата фиксации: 2026-07-31 (Europe/Moscow)

## 0. Причина поправки

Скаут (v8-epoch-life) в четырёх независимых прогонах не нашёл ни одного
длинноживущего (life >= 22 мин) движущегося бинарного крипто-рынка:
long_lived_movers = 0. Всё наблюдаемое движение сосредоточено в коротких
рынках up/down (5m/15m). Длинного движущегося рынка структурно нет,
ширина выборки исчерпана на трёх параллельных активах. Поэтому объём
набирается ВРЕМЕНЕМ ВДОЛЬ ЦЕПОЧКИ (качение на следующий
epoch-рынок), а не шириной по рынкам.

## 1. Метод: качение по параллельным цепочкам

Три параллельные цепочки по активам: btc, eth, sol. Каждая цепочка на
каждом цикле заново выбирает ТЕКУЩИЙ живой короткий up/down рынок своего
актива (epoch <= now < epoch+dur, active & acceptingOrders & not closed,
строго бинарный) и снимает его книгу один раз. Когда рынок
закрывается, следующий цикл автоматически берёт следующий epoch-рынок
той же цепочки (это и есть качение).

Срок/эпоха определяются ТОЛЬКО по слагу вида updown-Nm/Nh-<epoch>, где
epoch = НАЧАЛО окна рынка. По слагу -et время НЕ определяется (дефект
парсера -et признан; -et рынки в выборе времени не участвуют).

## 2. Объём захвата (ФИКСИРУЕТСЯ ЗАРАНЕЕ)

3 цепочки x 120 циклов x 30 с = 360 снимков.
Остановка: СТРОГО ПО ОБЪЁМУ, не по достижению n_div. Это исключает
выборочную остановку (optional stopping): нельзя вести качение до тех
пор, пока n_div не доберётся до 30.

Обоснование объёма: из 360 снимков разогрев 60 с на каждом слоте срезает
около пятой части, ещё часть уходит в no_etalon из-за редких сделок;
останется порядка 250 годных. Порог n_div >= 30 требует от них ~12%
расхождений. При 120 снимках годных было бы 70-90, и порог требовал бы
больше трети — тот же тупик, из-за которого отвергнуты дневные рынки.

## 3. Разогрев (warmup)

Первые 60 с жизни каждого слота (рынка) исключаются: снимки, у которых
capture_ts - epoch_start < 60, помечаются как warmup (Counts.n_warmup) и НЕ
входят в знаменатель (n_aligned/n_divergent).

## 4. Эталон

last_trade = последняя РЕАЛЬНАЯ сделка из /trades (data-api), фильтр по
полю asset = token_id, с timestamp <= времени снимка и в окне 90 с. Если
такой сделки нет — снимок идёт в n_no_etalon (не промах, из знаменателя
исключается). book.last_trade_price из /book НЕ используется как эталон.

## 5. Критерий отбора рынка

- строго бинарный: 2 исхода И 2 clobTokenId И сумма outcomePrices ~ 1
  (допуск 0.02);
- mid в [0.10, 0.90];
- движение mid >= 2 тиков за 5 мин;
- >= 1 реальная сделка за 5 мин;
- порога по ЧАСТОТЕ сделок нет (частотный фильтр отброшен: он срезает
  самые информативные рынки, а устаревание уже гасится правилом 90 с
  no_etalon).

## 6. Решающее правило (без изменений)

Расходящийся снимок: |mid - last_trade| > tick_frac * tick,
tick_frac = 0.5.
Пороги: min_divergent = 30, match_rate_min = 0.90, cross_rate_max = 0.10,
align_window_s = 90.
decide() применяется ОДИН РАЗ к суммарным totals по всем рынкам цепочек:
- n_aligned == 0 -> no_data;
- n_divergent < 30 -> inconclusive (ЧЕСТНЫЙ замороженный исход, НЕ повод
  продолжать качение; более длинный захват возможен лишь как НОВЫЙ
  заранее зафиксированный объём);
- match_mid_rate >= 0.90 и match_last_trade_rate <= 0.10 -> book_mid;
- иначе inconclusive.

Наблюдения внутри цепочки считаются независимыми для вопроса Э1;
кластерные ошибки не применяются.

## 7. Гарантия честности

data/e1_result.json пишется ТОЛЬКО при чистом рабочем дереве
(Поправка 2 закоммичена) и штампуется preregistration_commit = git HEAD.
На грязном дереве результат не пишется.




# Поправка 4 к PREREGISTRATION.md

Дата: 2026-08-01
Коммит поправки: 1e77f257e14fa6cfb097c1524291c9ad402e1307
Основание-ссылка: результат Э1 — коммит a1c81b7 (data/e1_result.json, 360 снимков, n_div=191)

## 0. Журнал: статус гейта G1 после Э1

Гейт G1: не разрешён. Э1 дал inconclusive при n_div=191. prices-history не отслеживает ни book-mid (8.9%), ни последнюю сделку (7.9%); 83% неоднозначны.

Альтернативное объяснение (не проверялось): fidelity=1 даёт минутную сетку, а наши снимки шли раз в 30 с, поэтому часть неоднозначности может быть артефактом выравнивания. Не проверялось, потому что источник prices-history выводится из проекта — см. §1.

Закрыта только задача: прогон проведён по замороженному протоколу и зафиксирован (коммиты 643cdc9 заморозка Поправки 2, 1e55ff8 штамп хэша, a1c81b7 результат). Вердикт inconclusive воротами G1 не является разрешением.

## 1. Поправка 4: prices-history выведен из состава источников цен

Основания (оба зафиксированы):
а) Э1 показал отсутствие разбираемой семантики ряда prices-history (коммит a1c81b7, 360 снимков, n_div=191);
б) для закрытых рынков prices-history отдаёт данные с гранулярностью от 12 часов и грубее (Polymarket py-clob-client, issue #216). Теннисный матч живёт около двух часов, поэтому ретроспективный расчёт цен из этого источника невозможен в принципе.

Замена источников цен:
- Ретроспектива (Этап 4): цены берутся только из /trades (ончейн), без схлопывания/децимации.
- Реальное время (Этап 1 и далее): собственные снимки стакана.

Статус ворот G1:
Ворота G1 существовали потому, что Этап 4 планировал брать цены из prices-history. С удалением этой зависимости ворота G1 перестают что-либо ограничивать. Это удаление зависимости, а не переобъявление отрицательного результата положительным.

Что НЕ меняется:
Пороги, правило GO, K=2.5, q=0.10 и порядок исходов остаются прежними.

