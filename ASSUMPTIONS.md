# ASSUMPTIONS.md -- где ваши допущения могут быть неверны

Маркировка: **(а)** подтверждено документацией или кодом, который я вижу;
**(б)** вероятно; **(в)** предположение.

Важное предупреждение о статусе всего документа: у меня нет доступа к
живой документации Polymarket и к исходникам `polymarket-client` в этой
сессии. Поэтому все ваши инфраструктурные факты в коде использованы как
вводные данные (вы велели не предполагать иное), но в этом файле они
проверяются на логическую уязвимость. "Не предполагать иное" -- рабочее
правило для кода, но оно не делает факт верным.

---

## A1. "Отображаемая цена = mid, но при спреде > $0.10 -- последняя сделка"

**Статус:** (в) в части того, что это правило переносится на API.

**В чём риск.** Это правило описывает поведение ФРОНТЕНДА. Из него НЕ
следует, что так же ведёт себя `/prices-history`. Возможны три разные
семантики одновременно: цена на графике, цена в списке рынков (Gamma
`lastTradePrice`/`bestBid`/`bestAsk`) и цена в истории. Гибридное правило
с порогом по спреду создаёт четвёртый вариант, который Э1 в его текущей
форме обнаружит как `inconclusive`: часть точек будет mid, часть -- last trade,
и сумма комплементов даст бимодальное распределение с умеренной сигмой.

**Как проверить.** Разбить выборку Э1 на две группы по одновременно
измеренному спреду (`WIDE_SPREAD_THRESHOLD = 0.10`) и считать сигму ОТДЕЛЬНО
в узкой и широкой группах. Если сигма резко выше только в широкой --
гибридное правило подтверждено, и вердикт Э1 нужно расширить третьим
значением `hybrid_by_spread` ДО заморозки предрегистрации, а не после.
**Это единственная правка, которую я рекомендую внести до первого прогона.**

---

## A2. "C -- число долей" и одновременно "Э2 выясняет, что такое C"

**Статус:** противоречие в самом брифе.

**В чём риск.** В блоке "КРИТИЧЕСКИЕ ФАКТЫ" сказано "C -- число долей"
(т.е. факт, не предполагать иное), а в Э2 то же самое объявлено предметом
проверки. Одно из двух утверждений лишнее. Если оставить оба, то при
результате Э2 = `notional` возникнет соблазн сказать "значит, тест плохой".

**Как решено в коде.** `pm/fees.py` НЕ содержит зашитого ответа: `taker_fee()`
поднимает `FeeBasisUnresolved`, пока Э2 не запишет результат. До того
доступен только `fee_bracket()`. Так ваш факт не попадает в выводы через
чёрный вход.

**Как проверить.** Два независимых пути, их результаты должны совпасть:
1. API-путь: `/trades` с полем комиссии, только сделки с `|p - 0.5| >= 0.15`.
2. Ончейн-путь: `probe.py --e2 --tx 0x...`. Важно: я НЕ зашивал ABI
   `OrderFilled` по памяти. Функция выгружает сырые 32-байтовые слова лога
   и показывает их как hex / uint / uint÷10^6, чтобы вы сами сопоставили
   позиции с ABI из верифицированного контракта. Угаданное ABI здесь
   хуже отсутствия ABI.

Дополнительно непроверенное (в): что в формуле `p` -- цена КУПЛЕННОГО
токена, а не всегда YES. Функция p*(1-p) симметрична, поэтому ошибка
здесь НЕ обнаруживается численно никогда -- удобно для вас.

---

## A3. "Мейкеры не платят никогда"

**Статус:** (б) как текущая политика; (в) как постоянное свойство.

**В чём риск.** Слово "никогда" превращает параметр бизнес-модели в константу.
Если maker rebate/fee появится, все оценки `cost` смещаются, а код, где
`maker_fee()` возвращает литеральный 0, об этом не узнает.

**Как проверить.** Еженедельный автотест: взять свои мейкерские исполнения за
неделю и проверить, что сумма комиссии точно равна 0 и что поле комиссии
вообще ПРИСУТСТВУЕТ (отсутствие поля != нулевая комиссия). В `pm/fees.py`
`maker_fee()` сознательно сделана функцией, а не константой -- чтобы было
одно место для правки.

---

## A4. "Ставки: Crypto 0.07 / Sports 0.05 / Politics 0.04 / Geopolitics 0"

**Статус:** (в) в части того, что вертикаль однозначно выводится из данных.

**В чём риск.** Ставки могут быть верны, но НЕПРИМЕНИМЫ, если нельзя
надёжно отобразить рынок -> вертикаль. Теннис "очевидно" Sports, но
гибриды ("кто победит и будет ли травма", события вокруг ставок на турниры)
классифицируются неоднозначно. Разница 0.04 вс. 0.05 -- это 25% издержки.

**Как проверить.** `Market.vertical()` возвращает `None`, когда не уверен, а не
угадывает. Прогнать классификатор по всем теннисным рынкам и вывести долю
`None`. Если она выше 5%, вертикаль нужно брать из фактически списанной
комиссии (решить обратную задачу по feeRate), а не из тегов.

---

## A5. "/orderbook-history мёртв, возвращает пустое молча"

**Статус:** (б) для момента наблюдения; (в) для текущего момента.

**В чём риск — и он асимметричен.** Если эндпоинт оживёт или частично работает
для подмножества рынков, вы потеряли бесплатную историю. Но главное
другое: "пусто и молча" -- точно то же поведение, что у живого эндпоинта
с неверными именами параметров. Две гипотезы наблюдательно неразличимы.

**Как проверить.** `probe.py` в preflight разделяет тихую смерть (200 + пусто)
от громкого отказа (4xx/5xx) и пишет `silent_death_confirmed`. Отдельно
стоит перебрать 2-3 варианта имён параметров на заведомо ликвидном рынке
перед тем, как писать "мёртв" в отчёт цели C. Тот же риск учтён в Э1:
`_PARAM_VARIANTS` перебирает три комбинации имён для `/prices-history`, потому
что пустой ответ там НЕ доказывает отсутствие данных.

---

## A6. "Ончейн-отмены нет, есть операторский pauseUser"

**Статус:** (б).

**В чём риск.** Не в факте, а в трёх неизвестных, каждое из которых важнее
самого факта: (1) каков триггер pauseUser -- автоматический порог или
ручное решение; (2) обратима ли блокировка и через какой канал; (3) блокируется
адрес или аккаунт целиком (включая вывод средств). Ни на один из них
я не имею подтверждённого ответа, и в коде они помечены именно так.

**Как проверить без риска.** Не экспериментально. Спросить поддержку письменно
и поискать вызовы `pauseUser` в истории транзакций контракта в Polygon:
частота и наличие парных разблокировок дадут оценку обратимости без
участия вашего аккаунта. При одном аккаунте цена ошибки несимметрична:
выигрыш -- знание, проигрыш -- конец всех трёх целей одновременно.

---

## A7. "expiration убран из подписи EIP-712, но остался в теле POST /order"

**Статус:** (б) технически; но вывод, который из этого следует, вы не сделали.

**В чём риск -- это самый недооценённый пункт брифа.** Если `expiration` НЕ
входит в подпись, то GTD-срок не защищён криптографически и потому не
является гарантией: он становится просьбой к оператору, а подписанный
ордер остаётся технически исполнимым любым, кто его получил, бессрочно
(в пределах остальных полей подписи). Следствие: **гейтом безопасности
является не TTL, а фактический DELETE + подтверждённый пустой список**.
Именно поэтому в Э3 килл-свитч определён через DELETE-и-проверку,
а не через ожидание истечения.

**Как проверить.** Поставить ордер с TTL 60 с по заведомо неисполнимой цене,
НЕ отменять, дождаться 120 с и проверить `GET /orders`: исчез ли ордер
сам и КТО его убрал (статус `expired` вс. исчезновение без следа).
Граничные значения для минимального TTL: 1, 5, 10, 30, 60 с -- сервер должен
вернуть ошибку валидации ниже порога; текст ошибки и есть ответ.

---

## A8. "Сумма комплементарных токенов различит mid и last trade"

**Статус:** (б), но тест слабее, чем выглядит. Три способа его сломать:

1. **Сетки времени могут не совпадать.** Если у YES и NO разные наборы
   меток, любое выравнивание вводит дрейф, который вы примете за last trade.
   Закрыто в коде: только точное пересечение меток, форвард-заполнения нет,
   доля пересечения в отчёте.
2. **NO-токена может не быть в торгах вовсе.** В бинарных рынках торговля
   часто концентрируется в одном токене. Тогда "дрейф" есть просто
   устаревшая цена одной стороны, а не семантика эндпоинта.
   Проверка: требовать ненулевого объёма ПО ОБОИМ токенам в окне.
3. **Neg-Risk рынки.** Там сумма по набору исходов равна 1 не попарно,
   а по всей группе. Код берёт только `is_binary` рынки ровно с двумя
   токенами; включение Neg-Risk сломало бы тест молча.

Дополнительно: ваш порог "дрейф ±0.02-0.04" предполагает средний спред
порядка 4-8 центов. В тонких теннисных рынках спред бывает 20+ центов,
и тогда дрейф last-trade выйдет далеко за ваш диапазон. Поэтому в коде
вердикт `last_trade` ставится при `mean|dev| >= 0.02` БЕЗ верхней границы.

---

## A9. "Кластерные SE по событию достаточны"

**Статус:** (б) для одного трейдера; (в) для всей выборки.

**В чём риск.** Кластеризация по матчу снимает внутриматчевую корреляцию,
но НЕ снимает корреляцию МЕЖДУ матчами одного дня/турнира: общие корты,
общая линия букмекеров, общий источник информации у трейдера. С 30-50
событиями из 5 турниров у вас фактически 5 кластеров, а не 30, и SE
недооценён в ~2.5 раза. Это прямой путь к ложному GO.

**Как проверить.** Посчитать решающее правило дважды: кластер = матч и
кластер = турнир-день. Если вердикт меняется, обязательной версией
становится КОНСЕРВАТИВНАЯ (турнир-день). Зафиксируйте это правило
в PREREGISTRATION.md до прогона -- потом будет поздно.

---

## A10. "Единица наблюдения -- событие (матч)" в теннисе

**Статус:** (в) в части того, что одно событие = одна независимая ставка.

**В чём риск.** Внутриматчевая торговля теннисом -- это моментум внутри матча
(брейки, подача). Агрегация до матча может стереть реальный эффект (ошибка
II рода) так же легко, как его выдумать. Для вас это приемлемо (вероятный
исход NO-GO и так ожидается), но важно назвать вещи своими именами:
NO-GO будет означать "нет копируемого edge НА УРОВНЕ МАТЧА", а не
"edge нет". Это различие должно остаться в формулировке вердикта.

---

## A11. "Открытый датасет -- гарантированный результат"

**Статус:** (в).

**В чём риск.** Гарантирован сбор данных, не гарантирована публикация.
Два реальных ограничения: (1) ToS платформы о повторном распространении
данных -- я не знаю их текущего текста и не буду его выдумывать;
(2) публикация адресов с метками поведения -- это деанонимизация, даже
когда данные публичны.

**Как проверить.** Прочитать действующие ToS до первой публикации
и решить заранее: агрегаты без адресов или хеши адресов с солью.

---

## A12. Открытые вопросы SDK (почему pm/broker.py нет в архиве)

**Статус:** честное "данных не хватает".

Я не видел исходников `polymarket-client` в этой сессии и потому НЕ пишу
код его инициализации, подписи и вызовов по памяти: подписание ордера
с угаданными полями -- тот самый класс ошибок, который заканчивается
неудавшимся расчётом и `pauseUser`. Вместо этого в `e3_gtd_cancel.py` задан
ПРОТОКОЛ `OrderClient` (Protocol) из трёх методов. Вам нужно один раз
реализовать `pm/broker.py` под этот протокол, глядя в реальный SDK.

Неизвестные, на которые нужно ответить чтением кода SDK, а не догадкой:
1. Точное имя пакета в PyPI и импорт-путь (в requirements.txt оставлен
   комментарий вместо угаданного имени с версией).
2. Как передаётся `expiration` в теле и валидирует ли SDK минимальный TTL локально.
3. Есть ли пакетная отмена (cancel-all) и атомарна ли она -- это определяет
   качество килл-свитча из A7.
4. Какой адрес подписывает и какой финансирует (funder vs signer):
   ошибка здесь даёт ровно failed settlement.
5. Есть ли у pUSD отдельный allowance-менеджер и можно ли видеть
   текущий allowance без его изменения (только-чтение).

---

## Коротко: порядок устранения по соотношению цена/эффект

1. **A9** (кластер = турнир-день как вторая обязательная версия) -- бесплатно,
   предотвращает ложный GO.
2. **A1** (добавить вердикт `hybrid_by_spread` до заморозки) -- одна правка
   сейчас вместо спора с собой потом.
3. **A7** (TTL не есть гарантия) -- меняет дизайн килл-свитча.
4. **A2** (базис C) -- без него любой вердикт будет UNDECIDABLE.
5. **A5** (тихая смерть вс. неверные параметры) -- влияет на цель C.
6. **A6** (pauseUser) -- низкая вероятность, максимальный ущерб.

## Обновление 2026-07-31 — итоги probe (Этап 0, задание 03)

- A8 — ФАЛЬСИФИЦИРОВАНО. Сумма ног YES+NO = 1 по построению (сервер считает
  NO = 1 - YES); различителем базы цены быть не может. См. PROBE_RESULTS.md.
- Gamma /markets молча игнорирует tag_slug -> все прежние тегированные выборки
  были фиктивными. Замена: /events?tag_slug=... Негативный контроль пройден
  (несуществующий тег -> n=0).
- /markets режет страницу до 100 записей независимо от limit.
- Офсетная пагинация /events упирается в потолок ровно на offset 2000
  (2001 -> 422). Полный обход только нарезкой по датам (end_date_min/
  end_date_max) с проверкой, что срез не упёрся в потолок.
- Gamma /markets просрочен: deprecation:true, sunset 2026-05-01,
  warning 299 "use /markets/keyset". iter_markets держится только на нём ->
  технический долг (пункт 6 очереди).
- feeSchedule в метаданных: ставки подтверждены (политика 0.04, спорт 0.05,
  крипта 0.07; exponent 1; takerOnly). НО ребейты и число категорий (8, не 4)
  расходятся с записанным -> заменить fee_rate(vertical) на feeSchedule.rate
  per-market.

- 2026-07-31 (item 6): ФАКТ (probe) — /markets/keyset ТОЖЕ молча игнорирует tag_slug (tag_slug=tennis вернул политику 2020 г.). Тегированная фильтрация только через /events?tag_slug=. Реализовано: iter_events (нарезка по датам) + iter_markets_keyset (курсор next_cursor) + iter_markets(tag=...) теперь падает RuntimeError.

- 2026-07-31 (items 6-7 validated): smoke_e4.py против живого Gamma через pm.ReadClient, iter_events(tag=tennis, closed, 90d): singles=5257, doubles=1714, other=8074, утечка=0 (TAG_FILTER_OK). Сходится с probe (~5266/~1702). Клиентский путь подтверждён на данных.

<!-- ASSUMPTION-BLOCKTIME-2026-08-01 -->
## Допущение 2026-08-01 — block-time Polygon (поправка)

Измерено live через eth_getBlockByNumber (diag_getlogs_bench.py, latest_block=91238915): средний block-time ≈ 1.5 с на выборке 20000 блоков.
Следствие: **57 600 блоков/сутки**, 90 дней ≈ **5.18 млн блоков**.
Отменяет прежнюю оценку ~43 200 блоков/сутки (2 с/блок) — она была занижена примерно на треть.
Все расчёты диапазонов блоков (сканы логов, поиск блока по timestamp) используют 57 600/сутки.

<!-- ASSUMPTION-ORDERFILLED-DATA-2026-08-01 -->
## Dopushchenie 2026-08-01 -- OrderFilled raskladka (podtverzhdeno keccak256)

keccak256("OrderFilled(bytes32,address,address,uint8,uint256,uint256,uint256,uint256,bytes32,bytes32)")
= 0xd543adfd945773f1a62f74f0ee55a5e3b9b1a28262980ba90b1a89f2ea84d8ee -- SOVPADAET s nashim topic0.
Znachit raskladka iz dekodera poly_data -- FAKT, ne gipoteza:
- topics[0]=signature; topics[1]=orderHash (bytes32, NE adres);
  topics[2]=maker (address); topics[3]=taker (address).
- data (7 slov): w0=side (0=BUY,1=SELL, storona MEJKERA), w1=tokenId,
  w2=makerAmountFilled, w3=takerAmountFilled, w4=fee, **w5=builder (bytes32)**, **w6=metadata (bytes32)**.
Otmenyaet prezhnee "w5/w6=0 po opredeleniyu": eto builder/metadata (chasto nuli, no ne po opredeleniyu).
Adresa maker/taker indeksirovany (topics[2]/topics[3]) -> HyperSync filtruet po koshelku na svoey storone.

<!-- ASSUMPTION-OFFSET-CEILING-2026-08-02 -->
## Допущение 2026-08-02 — потолок offset у Gamma (проверочный контур)

Пишет проверочный контур. Решение владельца (задача 2, вписать в ASSUMPTIONS.md).

**ФАКТ (живые пробы 2026-08-02):** у Gamma потолок смещения (offset) ≈ 2000
на `/events` и на `/markets` (на /markets ранее наблюдали ~2100). Запрос с
offset выше потолка отдаёт 422. Это **системное свойство** Gamma, а не
разовый сбой. Любое перечисление списков должно учитывать потолок: нарезка
по датам / курсоры, а не полный offset-обход.

<!-- ASSUMPTION-BOOK-SERVERTS-2026-08-02 -->
## Допущение 2026-08-02 — /book: серверная метка и выравнивание сверки (поправка владельца)

Пишет проверочный контур. Записано дословно из поправки владельца (задача 2, принята):

- `/book` отдаёт серверный timestamp в мс эпохи, **всегда** (1761 из 1761 ответов).
- **Ответ `/book` — КЭШ, а не живой снимок** (задача 3.6, ФАКТ): для всех 240
  снимков число их событий в интервале [ts_server_ms, ts_recv_ms) систематически
  > 0: min=43, медиана=77.5, p90=124, max=246; доля снимков со счётчиком 0 = 0.0.
  Медиана book_age_ms = 763 (диапазон 748–1059). Это означает: на момент
  получения мы получаем состояние, которое уже устарело на десятки реальных
  событий (медиана их интервала между событиями 3 мс). Формулировка
  «фактическое отставание 0.75–1.7 с» ЗАМЕНЕНА на эту точную.
- Токены опрашиваются последовательно с шагом ~107 мс, поэтому **две стороны
  одного рынка НЕ являются одновременным снимком**.

Сверка выравнивается по **серверной метке `ts_server_ms`**, а не по `ts_recv_ms`.
У вебсокета и у REST метка в одних единицах (мс эпохи), часы общие. `ts_recv_ms`
остаётся только для диагностики задержки.

**Поправка к допуску сверки (из цифр прогона):** допуск выравнивания в 1 секунду
слишком узкий — он объявит потерянными обновления, просто пришедшие позже.
В задаче 4 (compare) допуск заменён: выравнивание идёт по ts_server_ms на
состояние книги, а не по событию; отставание кэша (см. выше) роли не играет,
т.к. и /book, и реконструкция дают одно и то же состояние на момент
ts_server_ms. Результат сверки: 100% точных совпадений на 240 снимках (задача 4).

**Отказы /book (ФАКТ 2026-08-02):** мёртвый рынок (окно закрылось) → **HTTP 404**,
тело `{"error":"No orderbook exists for the requested token id"}`. Это отдельная
история от 429 (лимит), которого в прогоне не было. Коллектор должен различать:
404 = рынок умер (считать наблюдением смены рынка), 429 = лимит (пауза/бэкофф).

<!-- ASSUMPTION-PMDATA-SIDE-2026-08-02 -->
## Допущение 2026-08-02 — pmdata: установленные факты (задача 3.5)

Пишет проверочный контур. Установлено эмпирически (задача 3.5, принята).

**Факты о формате pmdata (проверены на файле btc-updown-15m-1785653100.parquet):**
- Эпоха в слаге up/down — это **КОНЕЦ окна**, не начало (1785646800 = ровно
  05:00 UTC, данные с 04:45). Для btc-1785653100: окно 06:30–06:45, эпоха = 06:45.
- pmdata пишет **с открытия окна и ещё около 16 минут после закрытия, до резолва**
  (последнее событие `event_type=market_resolved`). Диапазон btc: 06:29:55–07:01:54.
- У них `timestamp` в **микросекундах** (`timestamp[us]`, эпоха UTC), у нас в
  **миллисекундах** (`ts_server_ms`/`ts_recv_ms`). Приведение: их мкс → мс
  явным делением на 1000 (автоприведение не использовать).
- В файле pmdata **отсутствует идентификатор исхода** (ни token_id, ни asset_id,
  ни Up/Down) — есть только `market_slug`. Сторона определяется только сопоставлением.

**Результат задачи 3.5 — чью сторону пишет pmdata (ФАКТ):**
Наложение по серверной метке (их мкс→мс, последнее событие ≤ нашего ts_server_ms)
на рынке btc-updown-15m-1785653100 (n=120 совпадений на токен, lag=0.0 мс):

| наш токен | outcome | mean \|our_bid − their_bid\| | mean \|our_bid − (1−their_ask)\| |
|---|---|---|---|
| 7880…1394 | Up | **0.000000** | 0.275333 |
| 1020…2657 | Down | 0.276000 | **0.000000** |

Вывод: pmdata пишет книгу **стороны Up напрямую** (их best_bid/best_ask =
наша книга Up-токена), а сторона Down выражается через комплемент
(наша bid_Down = 1 − их ask). Сопоставление в сверялке (задача 4):
- наш Up-токен сравнивается с их `best_bid`/`best_ask` напрямую;
- наш Down-токен — через `(1 − their_ask)`/`(1 − their_bid)`.
Установление Up/Down в наших данных: по gamma-слагу порядок clobTokenIds
совпадает с порядком outcomes (["Up","Down"]).

**Плотность потока (ФАКТ):** ~28 событий `price_change`/с на крипто-рынке up/down
(на порядки выше, чем на теннисе). На btc-файле: 55.4/с по всему диапазону,
117.1/с в фазе резолва (06:45–07:00); активное окно торговли 06:30–06:45 —
лишь ~1.5/с (по 06:15–06:30 вообще 0). Поток событий в секунду для задачи 4
НЕ сопоставим с нашим снимком раз в 5 с — сравнивать можно только состояние книги.

**Порядок массивов книги (ФАКТ):** `ask_prices`/`ask_sizes` — **по возрастанию**
цены (лучший ask = элемент [0]); `bid_prices`/`bid_sizes` — **по убыванию**
(лучший bid = элемент [0]). `best_ask` = ask_prices[0], `best_bid` = bid_prices[0].
Для события `book` поля best_bid/best_ask равны NULL (цены только в массивах),
для `price_change` — заполнены, массивы NULL.

<!-- ASSUMPTION-PMDATA-SIZES-2026-08-02 -->
## Допущение 2026-08-02 — pmdata: размеры и pc_side (задачи 3.6-недоделка, 4)

Пишет проверочный контур.

**Размеры (ФАКТ, задача 3.6-недоделка):** гипотеза перестановки сторон
подтверждена — для Down `bid_size_down = ask_size_up`, `ask_size_down =
bid_size_up` (комплемента для размеров не существует). Измерено на
btc-updown-15m-1785653100, n=120 на токен:

| токен | outcome | mean \|our_bid_size − their_bid_size\| | mean \|our_bid_size − their_ask_size\| |
|---|---|---|---|
| 7880…1394 | Up | **0.0** | 84.27 |
| 1020…2657 | Down | 84.23 | **0.0** |

(и зеркально по ask_size: для Up 0.0 против their_ask_size, для Down 0.0
против their_bid_size).

**Семантика `pc_side` в их price_change (ФАКТ, задача 4):** `BUY` меняет
**bid**-сторону книги, `SELL` — **ask**-сторону. Проверено на 792 парах
«book → дельты → book»: нулевая ошибка восстановления при этом сопоставлении,
в альтернативном (BUY→ask) ошибка 678.32 по best_bid+best_ask.

**Сверка задача 4 (ФАКТ):** выравнивание по ts_server_ms (их мкс → мс явным
делением), сторона Down через комплемент по правилу 3.5/3.6. На 240 снимках
(120 Up + 120 Down) по всем 5 величинам (best_bid, best_ask, spread,
vwap_bid_100, vwap_ask_100): точных совпадений 100%, медиана |Δ|=0, p99=0,
расхождений > 1 тик = 0. Восстановление книги из дельт реализовано как
переиспользуемый `BookReconstructor` (src/validate/compare.py).

## 2026-08-02 — data-api/positions: только открытые позиции

- data-api/positions отдаёт ТОЛЬКО открытые позиции.
- Проверено на кошельке 0x03dc85f8...: 307 рынков в /trades, 8 в /positions, отсутствуют 299, обратных случаев 0.
- Поля прибыли (cashPnl, realizedPnl, avgPrice, size) даются по каждому рынку, но только для открытых.
- Следствие: Фильтр 4 через /positions невозможен; скан цепочки в плане остаётся.
- Постраничности нет: limit до 1000 отдаёт тот же результат, offset за пределом даёт пусто.
- Побочно: /positions отдаёт oppositeAsset — готовое соответствие токена и комплемента.
- Источник: probes/glm/probe2_run.log, probes/glm/probe2_summary.json

<!-- ASSUMPTION-FANOUT-DEEPSEEK-2026-08-02 -->
## Рабочая гипотеза (НЕПРОВЕРЕННАЯ) — 2026-08-02, deepseek

Статус: (в) предположение, НЕ проверена. Помечено как непроверенное, чтобы не сойти за факт.

**Гипотеза:** рост `recons_mismatch` на двух соединениях (28.6% с выключенным дедупом, 16% с включённым против 2.8% на одном) вызван не веерной рассылкой (fan-out НЕ подтверждён — см. PROBE_RESULTS.md), а конфликтом в параллельной записи/порядке приёмки между двумя websocket-соединениями коллектора: события, применяемые к одной книге из двух потоков, приходят и записываются не по одному глобальному порядку, что раздувает расхождения recon.

Числа, которые гипотеза должна объяснить (не факты, а то, что гипотеза призвана объяснить):
- 28.6% (двухния, dedup OFF) против 2.8% (одно), дедуп 16% против 28.6% — дедуп, ожидаемо повышающий точность, здесь её повышает, направления противоречит наивному ожиданию;
- 372821 событий против 201571 на тех же 56 токенах — двухпоточный поток почти вдвое «богаче», чем одно;
- 114 дельт за 500 мс с колеблющимися размерами по одной цене.

Альтернатива, которую нельзя различать не проверяя: дедуп пассивно выбрасывает часть комплементных дельт, попутно стирая расхождения. Проверка не проведена, поэтому гипотеза остаётся (в).

---

## Записи, добавленные после 2026-08-03

Восстановлено из коммита 8e30123 после того, как 815f161 перезаписал файл
и удалил ранее зафиксированные измерения. Ниже — содержимое, написанное
после этой даты, сохранено дословно.

# ASSUMPTIONS.md

## Фактические assumptions из логов — 2026-08-03

- **[подтверждено логом]** `/trades` с `offset>10000` может возвращать HTTP 400 для отмеченного рынка; полнота хвоста pre-match автоматически не следует. Источник: `logs/collect_dryrun.log`, `logs/collect_window.log`.
- **[подтверждено логом]** В проверенном выводе `/positions` `limit500=8`, `limit1000=8`, `limit1000_offset_n=0`, `offset5000=0`. Это не подтверждает поведение для всех аккаунтов. Источник: `probes/glm/probe2_run.log`, `probes/glm/probe2_summary.json`.
- **[подтверждено логом]** В просмотренном websocket-наборе timestamp встречался `174` раз, sequence-number field не обнаружен. Это не доказывает отсутствия sequence number в других типах сообщений. Источник: `logs/_ws_analysis.txt`.
- **[вероятно]** Единицей timestamp websocket является epoch milliseconds: лог прямо помечает это как `guessed units: epoch_milliseconds`, а не как независимую верификацию. Источник: `logs/_ws_analysis.txt`.
- **[подтверждено логом]** `unknown_types=0` не означает корректную реконструкцию: в smoke run `recons_mismatch=12`, в tennis run `recons_mismatch=100`. Источник: `logs/collector_smoke.log`, `logs/probe_tennis.log`.
- **[подтверждено логом]** В neg5 run измерены `dropped=559` и `recons_mismatch=47`; этот лог нельзя считать доказательством отсутствия потерь. Источник: `logs/neg5_run.log`.
- **[подтверждено логом]** Для pullstart-прогона `5393/5393 slugs`, `gameStartTime present=5393`, `null=0`, `failed_slugs=0`; это свойство конкретного запуска, а не универсальная гарантия API. Источник: `logs/filter7_pullstart.log`.
- **[подтверждено логом]** В feasibility-выводе ITF имеет `0` строк и `nan` по производным полям; ITF-метрики этим прогоном не измерены. Источник: `logs/filter7_feasibility.log`.
- **[вероятно]** `VERDICT: ALL GREEN on judged tiers` означает зелёный статус только для judged tiers: тот же лог сообщает `NO survivor stats computed (no gross_delta / no t / no pass counts)`. Источник: `logs/filter7_degeneracy.log`.
- **[предположение]** Ненулевой размер файла недостаточен для признания прогона завершённым: фактические traceback и обрывы есть в `probes/glm/probe1.log`, `probes/glm/probe1_window2.log`, `probes/glm/probe3_run.log`, `probes/opus/probe3b_run.log`. Источник: указанные логи.

## Факт о hash в price_change — 2026-08-03

- **[подтверждено логом]** Поле `hash` в элементе `price_changes` НЕ уникально: сервер переиспользует одно и то же значение hash для РАЗНЫХ изменений одного `asset_id` в пределах одной миллисекунды (два отдельных сообщения с одинаковым `recv_ms`). Источник: `logs/ws_raw.jsonl`, пара `recv_ms=1785651207744` — `price=0.3 size=8700` против `price=0.4 size=6781.44` с одним hash `5d37d11d...`.
- **[подтверждено логом]** По всему захвату `logs/ws_raw.jsonl`: 8 коллизий ключа `(asset_id, hash)` на 358 price_change-событий; из них 6 — РАЗНЫЕ изменения (разные price/size) с общим hash, 2 — дословные копии. То есть ключ дедупа обязан включать price и size: `(asset_id, hash, price, size)` — иначе легитимная дельта отбрасывается.
- **[подтверждено логом]** Данные суток от 2026-08-03 собраны СТАРЫМ ключом `(asset_id, hash)`: около 1.7% дельт не применены к книге; независимый контроль recon при этом `6/932` (0.6%) — расхождение с эталоном в пределах шума, массовой потери книги нет. Источник: прогон 2026-08-03, `logs/` (recons_mismatch 6 из 932).

## Дополнение по сырым сделкам и recon_check — 2026-08-05

- **[подтверждено данными]** Поле `tier` в `data\trades_raw` хранится в нижнем регистре: `'atp'`, `'wta'`, `'itf'`.
- **[подтверждено структурой данных]** `pairs.parquet` содержит агрегаты, а не отдельные сделки. Поэтому поправка, требующая пересчёта на уровне отдельной сделки, требует повторной выкачки сырых данных; пересчёт окна без обращения к API невозможен.
- **[подтверждено кодом `src\collect\recon.py`]** `max_abs_diff_price` сравнивает только лучший bid и лучший ask, строки 132-148; `max_abs_diff_size` сравнивает только пересечение цен `set(ours) & set(theirs)`, строки 150-156. `verdict="match"` ставится при равенстве числа уровней и нулевых обеих разницах, строки 160-164. Поэтому `match` не доказывает совпадение книг: два стакана с разными глубокими уровнями при равном числе уровней могут получить `match`. Наша сторона фильтруется по `s > 0`, строки 37-39; серверная сторона в исходном коде не фильтровалась — это отдельный дефект, исправленный в текущей сдаче.

### Опровержение ошибочной оценки объёма — 05.08.2026

Измерение повторено на полной выкачке: `3336,4 МиБ / 4299928 строк = 814 байт на строку`. Ранее записанное значение `776` признано неверным: это результат деления на `10^6` вместо `2^20`; корректное значение — `814`.
