# AUDIT_REPORT
Дата: 2026-07-31
Коммит на момент аудита: 317506fd343f642ece56b9d084e72e260f4c94f0

## 1. Тесты
Результат unittest:
35 tests ran, OK.
Результат import probe:
Падение на `probe.py:41` — `ImportError: cannot import name 'e2_fee_basis' from 'pm.experiments' (C:\Users\awf\Desktop\test\pm\experiments\__init__.py)`.

## 2. Контракт отсутствующего модуля e2_fee_basis
Импорт и вызовы из `probe.py`:
- `probe.py:41` — `from pm.experiments import e2_fee_basis as e2`
- `probe.py:186` — `e2.run(s, data, vertical=Vertical(args.vertical))`
- `probe.py:187` — `e2.report_dict(r2)`
- `probe.py:193` — `e2.decode_orderfilled(s.rpc_url, args.tx)`
- `pm/experiments/__init__.py:9` — модуль должен экспортироваться как `e2_fee_basis`

Ожидаемые формы по вызовам:
- `run(s, data, vertical=Vertical(...))` — принимает объект сессии `s`, данные `data`, параметр `vertical`; возвращает объект результата `r2`, пригодный для `report_dict`.
- `report_dict(r2)` — принимает результат `run` и возвращает словарь для отчёта.
- `decode_orderfilled(s.rpc_url, args.tx)` — принимает RPC URL и tx-хэш/идентификатор транзакции; возвращает декодированную on-chain информацию для отчёта.

Требует подтверждения при реализации:
- точные поля объекта результата `r2`;
- точная форма возвращаемого значения `decode_orderfilled`;
- ожидаемые исключения и их типы.

## 3. Нарушения нумерации гейтов
| файл | строка | сейчас | должно быть |
|---|---:|---|---|
| `pm/experiments/e4_tennis.py` | 102 | `Гейт G1 НЕ ПРОЙДЕН` для гейта мощности | `Гейт G4 НЕ ПРОЙДЕН` |
| `TASK.md` | 112-114 | упоминание, что `power_note` возвращает `Гейт G1 НЕ ПРОЙДЕН` для гейта мощности | должно быть помечено как `G4` |
| `TASK_AUDIT.md` | 74-75 | то же описание ошибки про `Гейт G1 НЕ ПРОЙДЕН` | должно быть `G4` |

## 4. Гейт мощности
Может ли сработать: нет
Обоснование:
`MIN_EVENTS_PER_TRADER = 30` сравнивается с общим числом разрешённых событий на рынке в `e4_tennis.py`, а не с количеством событий на одного трейдера. При таком сравнении порог почти наверняка всегда пройден, потому что рынок обычно содержит намного больше 30 событий, чем один трейдер. Значит, этот гейт не отделяет нужный случай и фактически не может работать как задумано.

## 5. Мелкие баги
1. resolved_in_window:
Подтверждено. В `pm/experiments/e4_tennis.py:147-159` цикл возвращает результат на первом же ключе, у которого значение — строка. Если первый найденный ключ не тот, остальные ключи уже не проверяются.
2. пагинация Gamma:
`page_limit=500`, `max_pages=40` дают максимум 40 запросов к `/markets`. Это укладывается в лимит Gamma 300 запросов / 10 секунд по числу запросов, если не учитывать дополнительные вызовы.
3. мёртвый DDL в store.py:
DDL для `e1_points`, `e1_pair_sums`, `e4_tennis_markets` есть в `pm/store.py:56-80`. `connect()` определён в `pm/store.py:83-100`, но по grep в репозитории не вызывается ниоткуда. Значит, этот DDL сейчас выглядит неиспользуемым.
4. hybrid_by_spread и кластеризация по турнирным дням:
В рамках этого аудита не проверялись; считать это вне области аудита и не считать подтверждённым.

## 6. Что нужно сделать (упорядочено по блокирующей силе)
1. Добавить отсутствующий модуль/экспорт `pm.experiments.e2_fee_basis`, чтобы `import probe` проходил.
2. Зафиксировать правильную нумерацию гейтов в тексте и коде: мощности — это G4, не G1.
3. Исправить логику/интерпретацию гейта мощности так, чтобы он сравнивал метрику на уровне трейдера, а не рынка.
4. Устранить ранний выход из `resolved_in_window`, если нужен полный перебор ключей.
5. Проверить, должен ли DDL из `pm/store.py` жить дальше; сейчас он не используется.

## 7. Вопросы, на которые я не имею права ответить сам
Таких нет.
